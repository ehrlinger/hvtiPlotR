# Numbers-at-Risk Table Harness Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a numbers-at-risk table renderer (`hv_atrisk()`) and a curve-over-table composer (`hv_atrisk_compose()`) so callers can build the standard Kaplan-Meier-plus-risk-table figure, plus a shared internal count helper (`.atrisk_table()`).

**Architecture:** One new file `R/at-risk-table.R`. `.atrisk_table()` computes the empirical at-risk count `sum(time >= t)` per stratum/report-time from subject-level data. `hv_atrisk()` resolves three input modes (a family object carrying `$tables$risk`; a precomputed `strata/time/n` data frame; or raw `(time,status,group)` columns) into one tidy risk table and renders it as a bare `ggplot2` panel. `hv_atrisk_compose()` aligns a curve plot's x-range onto the table panel and stacks them with `patchwork`. `hv_survival`'s existing `km_risk_table()` is left untouched (its survfit-derived numbers are authoritative).

**Tech Stack:** R, ggplot2, patchwork (all hard `Imports`), testthat, roxygen2/devtools.

---

## File Structure

- `R/at-risk-table.R` (new) — `.atrisk_table()` (internal), `hv_atrisk()` (exported), `hv_atrisk_compose()` (exported), and a small internal `.resolve_risk_df()` used by `hv_atrisk()`.
- `tests/testthat/test_at_risk.R` (new) — all tests.
- `NAMESPACE`, `man/*.Rd` — regenerated by roxygen.
- `vignettes/plot-functions.qmd`, `README.md`, `NEWS.md`, `DESCRIPTION` — docs + release.

Run tests for this file with:
`Rscript -e 'devtools::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test_at_risk.R")'`

---

### Task 1: `.atrisk_table()` count helper

**Files:**
- Create: `R/at-risk-table.R`
- Test: `tests/testthat/test_at_risk.R`

- [ ] **Step 1: Write the failing test**

Create `tests/testthat/test_at_risk.R`:

```r
# tests/testthat/test_at_risk.R
library(testthat)
library(ggplot2)

test_that(".atrisk_table counts subjects still at risk at each report time", {
  # 4 subjects, follow-up times 2, 4, 6, 8; report at 1, 5, 9
  tab <- .atrisk_table(time = c(2, 4, 6, 8), report_times = c(1, 5, 9))
  expect_equal(tab$strata, rep("Overall", 3))
  expect_equal(tab$report_time, c(1, 5, 9))
  expect_equal(tab$n.risk, c(4, 2, 0))   # >=1: all; >=5: {6,8}; >=9: none
})

test_that(".atrisk_table is non-increasing in time and splits by group", {
  tab <- .atrisk_table(
    time         = c(2, 8, 3, 9),
    group        = c("A", "A", "B", "B"),
    report_times = c(1, 5)
  )
  a <- tab$n.risk[tab$strata == "A"]
  b <- tab$n.risk[tab$strata == "B"]
  expect_equal(a, c(2, 1))   # A: >=1 {2,8}; >=5 {8}
  expect_equal(b, c(2, 1))   # B: >=1 {3,9}; >=5 {9}
  expect_true(all(diff(a) <= 0) && all(diff(b) <= 0))
})

test_that(".atrisk_table errors on bad input", {
  expect_error(.atrisk_table(time = numeric(0), report_times = 1),
               "non-empty numeric")
  expect_error(.atrisk_table(time = c(1, 2), report_times = NULL),
               "non-empty numeric")
  expect_error(.atrisk_table(time = c(1, 2), group = "A", report_times = 1),
               "same length")
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e 'devtools::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test_at_risk.R")'`
Expected: FAIL — could not find function `.atrisk_table`.

- [ ] **Step 3: Write minimal implementation**

Create `R/at-risk-table.R`:

```r
# at-risk-table.R
# Numbers-at-risk table harness: a shared empirical count helper, a renderer
# that turns a risk table into a bare ggplot panel, and a composer that stacks
# a survival curve over the table with aligned x-axes.
#
# `n.risk`, `report_time`, and `strata` are columns in the tidy risk table;
# suppress R CMD check notes about undefined globals used in aes().
utils::globalVariables(c("n.risk", "report_time", "strata"))

# ---------------------------------------------------------------------------
# Internal: empirical numbers-at-risk from subject-level data.
# n.risk at time t = number of subjects whose follow-up time is >= t.
# `status` is accepted for signature symmetry / future per-time event counts;
# v1 uses only `time` and `group`.
.atrisk_table <- function(time, status = NULL, group = NULL, report_times) {
  if (!is.numeric(time) || length(time) == 0L)
    stop("`time` must be a non-empty numeric vector.", call. = FALSE)
  if (!(is.numeric(report_times) && length(report_times) > 0L))
    stop("`report_times` must be a non-empty numeric vector.", call. = FALSE)
  if (is.null(group)) group <- rep("Overall", length(time))
  group <- as.character(group)
  if (length(group) != length(time))
    stop("`group` must be the same length as `time`.", call. = FALSE)

  strata_levels <- unique(group)
  rows <- lapply(strata_levels, function(st) {
    t_st <- time[group == st]
    do.call(rbind, lapply(report_times, function(rt) {
      data.frame(strata = st, report_time = rt,
                 n.risk = sum(t_st >= rt, na.rm = TRUE),
                 stringsAsFactors = FALSE)
    }))
  })
  out <- do.call(rbind, rows)
  rownames(out) <- NULL
  out
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e 'devtools::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test_at_risk.R")'`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add R/at-risk-table.R tests/testthat/test_at_risk.R
git commit -m "feat: add .atrisk_table() empirical numbers-at-risk helper"
```

---

### Task 2: `.atrisk_table()` agrees with `km_risk_table()` where conventions coincide

This task adds no code — it locks in the spec's claim that the only difference between the new helper and the existing KM table is the documented at-the-time convention, not a bug.

**Files:**
- Test: `tests/testthat/test_at_risk.R`

- [ ] **Step 1: Write the failing test**

Append to `tests/testthat/test_at_risk.R`:

```r
test_that(".atrisk_table matches km_risk_table when no events fall between report times", {
  # Events exactly at the report times -> both conventions agree.
  dta <- data.frame(iv_dead = c(1, 1, 2, 2, 3, 3), dead = rep(TRUE, 6))
  km  <- hv_survival(dta, time_col = "iv_dead", event_col = "dead",
                     report_times = c(1, 2, 3))
  emp <- .atrisk_table(time = dta$iv_dead, report_times = c(1, 2, 3))
  # Compare n.risk per report time (km strata label is the single-cohort name).
  expect_equal(emp$n.risk, km$tables$risk$n.risk)
})
```

- [ ] **Step 2: Run test to verify it passes (or fails informatively)**

Run: `Rscript -e 'devtools::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test_at_risk.R")'`
Expected: PASS. If it FAILS, the survfit `n.risk` at these exact-event report times differs from `sum(time >= t)` — stop and inspect `km$tables$risk` vs `emp` by printing both; do not "fix" by changing `km_risk_table()`. The expected values here are `c(6, 4, 2)` (at t=1 all 6; at t=2 the four with time>=2; at t=3 the two with time>=3).

- [ ] **Step 3: Commit**

```bash
git add tests/testthat/test_at_risk.R
git commit -m "test: pin .atrisk_table vs km_risk_table convention agreement"
```

---

### Task 3: `hv_atrisk()` — object and precomputed-data-frame input modes

**Files:**
- Modify: `R/at-risk-table.R`
- Test: `tests/testthat/test_at_risk.R`

- [ ] **Step 1: Write the failing test**

Append to `tests/testthat/test_at_risk.R`:

```r
test_that("hv_atrisk renders a panel from an hv_survival object", {
  dta <- sample_survival_data(n = 200, strata_levels = c("A", "B"), seed = 1)
  km  <- hv_survival(dta, time_col = "iv_dead", event_col = "dead",
                     group_col = "valve_type")
  p   <- hv_atrisk(km)
  expect_s3_class(p, "ggplot")
  # one text label per (stratum x report time)
  n_expected <- nrow(km$tables$risk)
  built <- ggplot2::layer_data(p, 1)
  expect_equal(nrow(built), n_expected)
})

test_that("hv_atrisk renders a panel from a precomputed risk data frame", {
  rdf <- data.frame(strata = c("A", "A", "B", "B"),
                    report_time = c(0, 5, 0, 5),
                    n.risk = c(50, 30, 40, 20))
  p   <- hv_atrisk(rdf)
  expect_s3_class(p, "ggplot")
  expect_equal(nrow(ggplot2::layer_data(p, 1)), 4L)
})

test_that("hv_atrisk accepts `n` and `time` column aliases", {
  rdf <- data.frame(strata = c("A", "B"), time = c(0, 0), n = c(10, 8))
  p   <- hv_atrisk(rdf)
  expect_s3_class(p, "ggplot")
  expect_equal(nrow(ggplot2::layer_data(p, 1)), 2L)
})

test_that("hv_atrisk strata_labels overrides row labels", {
  rdf <- data.frame(strata = c("A", "B"), report_time = c(0, 0),
                    n.risk = c(10, 8))
  p   <- hv_atrisk(rdf, strata_labels = c(A = "Group A", B = "Group B"))
  blt <- ggplot2::ggplot_build(p)
  ylabs <- blt$layout$panel_params[[1]]$y$get_labels()
  expect_true(all(c("Group A", "Group B") %in% ylabs))
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e 'devtools::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test_at_risk.R")'`
Expected: FAIL — could not find function `hv_atrisk`.

- [ ] **Step 3: Write minimal implementation**

Append to `R/at-risk-table.R` (the internal resolver + the renderer; the raw-data branch is added in Task 4, so resolve only object + data-frame modes here):

```r
# ---------------------------------------------------------------------------
# Internal: resolve any accepted input into a tidy risk data frame with
# columns strata, report_time, n.risk.
.resolve_risk_df <- function(x, time, status, group, report_times) {
  # Mode 1: an hv_data object carrying $tables$risk (e.g. hv_survival).
  if (inherits(x, "hv_data")) {
    rdf <- x$tables$risk
    if (is.null(rdf))
      stop("This object has no `$tables$risk`. Pass subject-level data with ",
           "`time`/`status`/`group`, or a precomputed risk data frame.",
           call. = FALSE)
    return(rdf)
  }

  if (!is.data.frame(x))
    stop("`x` must be an hv_data object, a precomputed risk data frame ",
         "(strata/time/n columns), or a subject-level data frame plus ",
         "`time` (and optional `status`/`group`).", call. = FALSE)

  # Mode 3 (raw subject-level data) is handled in Task 4 when `time` is given.
  if (!is.null(time)) return(NULL)  # placeholder; replaced in Task 4

  # Mode 2: precomputed risk data frame. Normalise column aliases.
  nm <- names(x)
  time_col <- if ("report_time" %in% nm) "report_time" else
    if ("time" %in% nm) "time" else NULL
  n_col    <- if ("n.risk" %in% nm) "n.risk" else
    if ("n" %in% nm) "n" else NULL
  if (!("strata" %in% nm) || is.null(time_col) || is.null(n_col))
    stop("Precomputed risk data frame needs `strata`, ",
         "`report_time` (or `time`), and `n.risk` (or `n`) columns.",
         call. = FALSE)
  data.frame(
    strata      = as.character(x[["strata"]]),
    report_time = x[[time_col]],
    n.risk      = x[[n_col]],
    stringsAsFactors = FALSE
  )
}

#' Numbers-at-risk table panel
#'
#' Renders a numbers-at-risk table as a bare \pkg{ggplot2} panel, ready to
#' stack under a survival curve with \code{\link{hv_atrisk_compose}}.
#'
#' @param x One of: an \code{hv_data} object that carries \code{$tables$risk}
#'   (e.g. from \code{\link{hv_survival}}); a precomputed risk data frame with
#'   \code{strata}, \code{report_time} (or \code{time}), and \code{n.risk}
#'   (or \code{n}) columns; or a subject-level data frame supplied together
#'   with the \code{time} (and optional \code{status}/\code{group}) column
#'   names, from which counts are computed.
#' @param time,status,group Column names in \code{x} when \code{x} is a
#'   subject-level data frame. \code{time} triggers the raw-data path;
#'   \code{status} is reserved and currently unused; \code{group} splits the
#'   table into strata. Default \code{NULL}.
#' @param report_times Numeric time points for the columns. \code{NULL}
#'   (default) uses the object's own points, or — on the raw-data path —
#'   an even spread derived from the observed time range.
#' @param size Text size for the counts. Default \code{NULL} (3.5).
#' @param strata_labels Optional named character vector remapping stratum row
#'   labels. Default \code{NULL}.
#' @param ... Ignored; present for S3 consistency.
#'
#' @return A bare \code{\link[ggplot2]{ggplot}} object: one text label per
#'   stratum and report time, strata as y rows (first stratum on top), time on
#'   a continuous x with axis text blanked (the curve carries the axis).
#'
#' @seealso \code{\link{hv_atrisk_compose}}, \code{\link{hv_survival}}
#'
#' @examples
#' km <- hv_survival(sample_survival_data(n = 200, seed = 1))
#' hv_atrisk(km)
#'
#' @importFrom ggplot2 ggplot aes geom_text scale_y_discrete theme_minimal
#'   theme element_blank
#' @importFrom rlang .data
#' @export
hv_atrisk <- function(x, time = NULL, status = NULL, group = NULL,
                      report_times = NULL, size = NULL,
                      strata_labels = NULL, ...) {
  rdf <- .resolve_risk_df(x, time, status, group, report_times)

  # First stratum on top: reverse factor levels (ggplot puts level 1 at bottom).
  lev <- unique(rdf$strata)
  rdf$strata <- factor(rdf$strata, levels = rev(lev))

  y_labels <- if (is.null(strata_labels)) ggplot2::waiver() else
    function(br) ifelse(br %in% names(strata_labels), strata_labels[br], br)

  ggplot2::ggplot(
    rdf, ggplot2::aes(x = .data$report_time, y = .data$strata)
  ) +
    ggplot2::geom_text(
      ggplot2::aes(label = .data$n.risk),
      size = if (is.null(size)) 3.5 else size
    ) +
    ggplot2::scale_y_discrete(labels = y_labels) +
    ggplot2::theme_minimal(base_size = 12) +
    ggplot2::theme(
      panel.grid  = ggplot2::element_blank(),
      axis.title  = ggplot2::element_blank(),
      axis.text.x = ggplot2::element_blank(),
      axis.ticks  = ggplot2::element_blank()
    )
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e 'devtools::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test_at_risk.R")'`
Expected: PASS (object mode, precomputed mode, aliases, strata_labels).

- [ ] **Step 5: Commit**

```bash
git add R/at-risk-table.R tests/testthat/test_at_risk.R
git commit -m "feat: hv_atrisk() renders a panel from object or precomputed table"
```

---

### Task 4: `hv_atrisk()` — raw subject-level data path + derived report_times

**Files:**
- Modify: `R/at-risk-table.R` (replace the Task-3 placeholder branch in `.resolve_risk_df()`)
- Test: `tests/testthat/test_at_risk.R`

- [ ] **Step 1: Write the failing test**

Append to `tests/testthat/test_at_risk.R`:

```r
test_that("hv_atrisk computes from subject-level data via time/status/group", {
  dta <- sample_survival_data(n = 200, strata_levels = c("A", "B"), seed = 1)
  p   <- hv_atrisk(dta, time = "iv_dead", status = "dead",
                   group = "valve_type", report_times = c(0, 5, 10))
  expect_s3_class(p, "ggplot")
  expect_equal(nrow(ggplot2::layer_data(p, 1)), 6L)  # 2 strata x 3 times

  # Same as the equivalent precomputed table.
  ref <- .atrisk_table(time = dta$iv_dead, group = dta$valve_type,
                       report_times = c(0, 5, 10))
  got <- ggplot2::layer_data(p, 1)
  expect_setequal(as.numeric(as.character(got$label)), ref$n.risk)
})

test_that("hv_atrisk derives report_times from the time range when NULL", {
  dta <- sample_survival_data(n = 100, seed = 1)
  p   <- hv_atrisk(dta, time = "iv_dead")
  built <- ggplot2::layer_data(p, 1)
  expect_gt(nrow(built), 0L)
  # derived points lie inside the observed follow-up range
  expect_true(all(built$x >= min(dta$iv_dead) & built$x <= max(dta$iv_dead)))
})

test_that("hv_atrisk errors on an unresolvable input", {
  expect_error(hv_atrisk(42), "hv_data object")
  expect_error(hv_atrisk(data.frame(foo = 1)), "Precomputed risk data frame")
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e 'devtools::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test_at_risk.R")'`
Expected: FAIL — the raw-data path returns the `NULL` placeholder, so `hv_atrisk()` errors before rendering.

- [ ] **Step 3: Replace the placeholder branch in `.resolve_risk_df()`**

In `R/at-risk-table.R`, replace this line in `.resolve_risk_df()`:

```r
  # Mode 3 (raw subject-level data) is handled in Task 4 when `time` is given.
  if (!is.null(time)) return(NULL)  # placeholder; replaced in Task 4
```

with:

```r
  # Mode 3: subject-level data frame + column names -> compute counts.
  if (!is.null(time)) {
    if (!(time %in% names(x)))
      stop("`time` column \"", time, "\" not found in `x`.", call. = FALSE)
    tv <- x[[time]]
    gv <- if (!is.null(group)) x[[group]] else NULL
    sv <- if (!is.null(status)) x[[status]] else NULL
    rt <- report_times
    if (is.null(rt)) {
      rng <- range(tv, na.rm = TRUE)
      rt  <- pretty(rng, n = 5)
      rt  <- rt[rt >= rng[1] & rt <= rng[2]]
      if (length(rt) == 0L) rt <- rng
    }
    return(.atrisk_table(time = tv, status = sv, group = gv,
                         report_times = rt))
  }
```

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e 'devtools::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test_at_risk.R")'`
Expected: PASS (raw-data path, derived report_times, error cases).

- [ ] **Step 5: Commit**

```bash
git add R/at-risk-table.R tests/testthat/test_at_risk.R
git commit -m "feat: hv_atrisk() raw subject-level data path with derived report_times"
```

---

### Task 5: `hv_atrisk_compose()` composer

**Files:**
- Modify: `R/at-risk-table.R`
- Test: `tests/testthat/test_at_risk.R`

- [ ] **Step 1: Write the failing test**

Append to `tests/testthat/test_at_risk.R`:

```r
test_that("hv_atrisk_compose stacks curve over table with shared x-range", {
  dta   <- sample_survival_data(n = 200, strata_levels = c("A", "B"), seed = 1)
  km    <- hv_survival(dta, time_col = "iv_dead", event_col = "dead",
                       group_col = "valve_type")
  curve <- plot(km)
  table <- hv_atrisk(km)
  comp  <- hv_atrisk_compose(curve, table)

  expect_s3_class(comp, "patchwork")
  # the two patches share x-axis limits after composition
  xr_curve <- ggplot2::ggplot_build(curve)$layout$panel_params[[1]]$x.range
  xr_table <- ggplot2::ggplot_build(comp[[2]])$layout$panel_params[[1]]$x.range
  expect_equal(xr_table, xr_curve, tolerance = 1e-6)
})

test_that("hv_atrisk_compose validates its inputs", {
  dta   <- sample_survival_data(n = 50, seed = 1)
  table <- hv_atrisk(hv_survival(dta))
  expect_error(hv_atrisk_compose("nope", table), "ggplot")
  expect_error(hv_atrisk_compose(table, "nope"), "ggplot")
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `Rscript -e 'devtools::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test_at_risk.R")'`
Expected: FAIL — could not find function `hv_atrisk_compose`.

- [ ] **Step 3: Write minimal implementation**

Append to `R/at-risk-table.R`:

```r
#' Stack a survival curve over a numbers-at-risk table
#'
#' Composes a curve plot over a \code{\link{hv_atrisk}} panel, aligning the
#' table's x-range to the curve's so the counts sit under the matching times,
#' and stacks them with \pkg{patchwork}.
#'
#' @param curve A \pkg{ggplot2} survival curve, e.g. \code{plot(hv_survival(...))}.
#' @param table A \pkg{ggplot2} numbers-at-risk panel from \code{\link{hv_atrisk}}.
#' @param heights Numeric length-2 curve:table height ratio. Default
#'   \code{c(3, 1)}.
#'
#' @return A \pkg{patchwork} object (the curve above, the table below).
#'   Decorate both panels with patchwork's \code{&}, e.g.
#'   \code{& theme_hv_manuscript()}.
#'
#' @seealso \code{\link{hv_atrisk}}
#'
#' @examples
#' km <- hv_survival(sample_survival_data(n = 200, seed = 1))
#' hv_atrisk_compose(plot(km), hv_atrisk(km))
#'
#' @importFrom ggplot2 ggplot_build coord_cartesian
#' @importFrom patchwork wrap_plots plot_layout
#' @export
hv_atrisk_compose <- function(curve, table, heights = c(3, 1)) {
  if (!inherits(curve, "ggplot"))
    stop("`curve` must be a ggplot object.", call. = FALSE)
  if (!inherits(table, "ggplot"))
    stop("`table` must be a ggplot object (from hv_atrisk()).", call. = FALSE)

  # Use the curve's already-expanded x-range as the table's hard range so the
  # two panels share identical x limits (expand = FALSE avoids re-padding).
  xr    <- ggplot2::ggplot_build(curve)$layout$panel_params[[1]]$x.range
  table <- table + ggplot2::coord_cartesian(xlim = xr, expand = FALSE)

  patchwork::wrap_plots(curve, table, ncol = 1) +
    patchwork::plot_layout(heights = heights)
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `Rscript -e 'devtools::load_all(quiet=TRUE); testthat::test_file("tests/testthat/test_at_risk.R")'`
Expected: PASS (composition, shared x-range, input validation).

- [ ] **Step 5: Commit**

```bash
git add R/at-risk-table.R tests/testthat/test_at_risk.R
git commit -m "feat: hv_atrisk_compose() stacks curve over aligned risk table"
```

---

### Task 6: Document, wire NAMESPACE, vignette + README, release

**Files:**
- Modify: `NAMESPACE`, `man/*.Rd` (generated), `vignettes/plot-functions.qmd`, `README.md`, `NEWS.md`, `DESCRIPTION`

- [ ] **Step 1: Regenerate roxygen docs + NAMESPACE**

Run: `Rscript -e 'devtools::document()'`
Expected: creates `man/hv_atrisk.Rd`, `man/hv_atrisk_compose.Rd`; `NAMESPACE` gains `export(hv_atrisk)` and `export(hv_atrisk_compose)`.

- [ ] **Step 2: Add a worked example to the survival section of `vignettes/plot-functions.qmd`**

Find the `hv_survival` section (search for `hv_survival(`). After its existing curve example, add a subsection:

````markdown
### Survival curve with a numbers-at-risk table

`hv_atrisk()` turns the `$tables$risk` an `hv_survival` object already carries
into a bare table panel, and `hv_atrisk_compose()` stacks the curve over it
with the time axes aligned. The non-KM curves (`hv_nonparametric`, `hv_ordinal`,
`hv_hazard`) take pre-summarised curve data and carry no risk table, so for
those you hand `hv_atrisk()` the subject-level `time`/`status`/`group` columns
instead.

```{r}
#| label: fig-km-atrisk
#| fig-width: 7
#| fig-height: 5
dta <- sample_survival_data(n = 400, strata_levels = c("Bioprosthetic",
                                                       "Mechanical"), seed = 7)
km  <- hv_survival(dta, time_col = "iv_dead", event_col = "dead",
                   group_col = "valve_type")

curve <- plot(km) +
  ggplot2::labs(x = "Years after operation", y = "Survival")
table <- hv_atrisk(km)

hv_atrisk_compose(curve, table) & theme_hv_manuscript()
```
````

- [ ] **Step 3: Add a SAS-migration note**

In `vignettes/sas-migration-guide.qmd`, find the survival / `%kaplan` section and add one line noting the precedent:

```markdown
The accompanying numbers-at-risk table — the SAS `tp.hp.dead.number_risk.R`
figure — is `hv_atrisk(km)`, stacked under the curve with
`hv_atrisk_compose()`; the counts are the same subjects-at-risk `PROC LIFETEST`
prints beneath its survival plot.
```

- [ ] **Step 4: Add a README gallery row**

In `README.md`, in the "Survival & Time-to-Event" table, add a row:

```markdown
| `hv_atrisk()` / `hv_atrisk_compose()` | Numbers-at-risk table panel, and a composer that stacks a survival curve over it with aligned time axes |
```

- [ ] **Step 5: Bump version and NEWS**

In `DESCRIPTION` line 4, change `Version: 2.3.4` to `Version: 2.5.0`.

In `NEWS.md`, add a new top section above the current top entry:

```markdown
# hvtiPlotR 2.5.0

## New features

- `hv_atrisk()` renders a numbers-at-risk table as a bare ggplot panel. It
  takes a survival-family object that carries `$tables$risk` (e.g.
  `hv_survival()`), a precomputed `strata`/`time`/`n` table, or a subject-level
  data frame plus `time`/`status`/`group` column names (the path for the
  curve-data constructors `hv_nonparametric`, `hv_ordinal`, `hv_hazard`, which
  carry no risk table). Missing `report_times` are derived from the observed
  time range.
- `hv_atrisk_compose()` stacks a survival curve over an `hv_atrisk()` panel,
  aligning the table's x-range to the curve's and composing them with
  patchwork. Decorate both panels with patchwork's `&`.

```

- [ ] **Step 6: Run the full suite and check**

Run: `Rscript -e 'devtools::document(); res <- devtools::check(args="--no-manual", quiet=TRUE); cat("E", length(res$errors), "W", length(res$warnings), "N", length(res$notes), "\n")'`
Expected: full test suite green; `R CMD check` 0 errors / 0 warnings on the package (pre-existing stray untracked files in the tree may add 1 warning + 1 note — those are not from this change).

- [ ] **Step 7: Commit**

```bash
git add NAMESPACE man/ vignettes/plot-functions.qmd vignettes/sas-migration-guide.qmd README.md NEWS.md DESCRIPTION
git commit -m "docs: document hv_atrisk()/hv_atrisk_compose(), bump to 2.5.0"
```

---

## Notes for the implementer

- `km_risk_table()` in `R/kaplan-meier.R` is deliberately **not** touched. Its survfit-derived `n.risk` is authoritative for KM; `.atrisk_table()` uses the standard `sum(time >= t)` convention and serves only the renderer's raw-data path. Task 2 pins where the two agree.
- `patchwork` and `ggplot2` are hard `Imports`, so no `requireNamespace()` guards or `skip_if_not_installed()` are needed in tests.
- `ggplot2::layer_data(p, 1)` is the supported way to read a layer's computed data in tests; for text layers the count is in the `label` column.
- Do not stage the stray untracked files (`.codegraph/`, `hazard_fixtures_key*`, `inst/extdata/Yahoo slide template LIGHT ROOM.pptx`).
- This branch (`feat/risk-table-composite`) is off `main`, independent of the open sankey PR. Open a PR when done; **John merges.**
