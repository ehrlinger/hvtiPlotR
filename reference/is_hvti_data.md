# Test whether an object is an hvtiPlotR data object

Test whether an object is an hvtiPlotR data object

## Usage

``` r
is_hvti_data(x)
```

## Arguments

- x:

  Any R object.

## Value

`TRUE` if `x` inherits from `"hvti_data"`, `FALSE` otherwise.
