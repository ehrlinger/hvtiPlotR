# Generate Sample Data for Mirrored Histogram

Creates a sample data frame suitable for testing plot_mirror_histogram.

## Usage

``` r
sample_mirror_histogram_data(n = 100)
```

## Arguments

- n:

  Number of samples per group (default 100).

## Value

Data frame with columns: prob_t (numeric score), tavr (group), match
(matched status)
