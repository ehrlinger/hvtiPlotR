# Build bare cumulative hazard ggplot

Build bare cumulative hazard ggplot

## Usage

``` r
km_build_cumhaz_plot(km_df, alpha)
```

## Arguments

- km_df:

  Tidy KM data frame.

## Value

A bare `ggplot` object.
