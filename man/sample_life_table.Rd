% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard-plot.R
\name{sample_life_table}
\alias{sample_life_table}
\title{Sample Population Life Table Data}
\usage{
sample_life_table(
  age_groups = c("<65", "65-80", "≥80"),
  age_mids = c(55, 72, 85),
  time_max = 10,
  n_points = 100
)
}
\arguments{
\item{age_groups}{Character vector of age group labels. Default
\code{c("<65", "65-80", "\\u226580")}.}

\item{age_mids}{Numeric vector of representative ages (years) for each
group, same length as \code{age_groups}. Default \code{c(55, 72, 85)}.}

\item{time_max}{Upper end of the time axis (years). Default \code{10}.}

\item{n_points}{Number of time points. Default \code{100}.}
}
\value{
A data frame with columns \code{time}, \code{survival}, and \code{group}.
}
\description{
Generates age-group-specific population survival curves using Gompertz
mortality, matching the US population life table overlays used in
\code{tp.hp.dead.age_with_population_life_table.sas} and
\code{tp.hp.dead.uslife.stratifed.sas}.
}
\details{
\strong{SAS column mapping:}
\itemize{
\item \code{time}     ← prediction time grid (years)
\item \code{survival} ← \code{SMATCHED} (age-group-specific survivorship, 0–100 \%)
\item \code{group}    ← age group label (e.g. \code{"<65"}, \code{"65-80"}, \code{"\\u226580"})
}
}
\examples{
lt <- sample_life_table(time_max = 10)
head(lt)
}
\seealso{
\code{\link[=hazard_plot]{hazard_plot()}}, \code{\link[=sample_hazard_data]{sample_hazard_data()}}
}
