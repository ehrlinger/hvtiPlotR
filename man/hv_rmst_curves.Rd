% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmst-plots.R
\name{hv_rmst_curves}
\alias{hv_rmst_curves}
\title{Prepare weighted Kaplan-Meier curves for an RMST figure}
\usage{
hv_rmst_curves(
  curves,
  estimates = NULL,
  tau = NULL,
  facet = "estimator",
  arm = "arm",
  time = "time",
  surv = "surv",
  ...
)
}
\arguments{
\item{curves}{A data frame of curve steps, or a \code{ps_rmst} object.}

\item{estimates}{Optional data frame with the \code{hv_rmst_contrast()} columns
(\code{estimator}, \code{diff_days}, \code{lo_days}, \code{hi_days}), used for the facet
annotations. If \code{NULL} and \code{curves} is a \code{ps_rmst}-like object, its
\verb{$tables$estimates} is used when present. \code{NULL} otherwise: no
annotation.}

\item{tau}{Optional single positive number: the RMST horizon, on the
same scale as \code{time}.}

\item{facet}{Name of the column defining facets. Default
\code{"estimator"}. Factor levels set the facet order.}

\item{arm}{Name of the arm column. Default \code{"arm"}. A character
column is ordered \code{"treated"}, \code{"control"}, then any other value.}

\item{time, surv}{Names of the time and survival columns. Defaults
\code{"time"} and \code{"surv"}.}

\item{...}{Ignored; present for S3 consistency.}
}
\value{
An object of class \code{c("hv_rmst_curves", "hv_data")}: a list with
\describe{
\item{\verb{$data}}{The curves data frame.}
\item{\verb{$meta}}{Named list: \code{facet}, \code{arm}, \code{time}, \code{surv} (column
names), \code{tau}, \code{facet_levels}, \code{arm_levels}, \code{n_missing}.}
\item{\verb{$tables}}{\code{shade}: the staircase polygon under each curve up to
\code{tau} (zero rows when \code{tau} is \code{NULL}); \code{labels}: one annotation per
facet (zero rows when \code{estimates} is \code{NULL}).}
}
}
\description{
Validates the step-function curves behind an RMST analysis and returns an
\code{hv_rmst_curves} object. Call \code{\link[=plot.hv_rmst_curves]{plot.hv_rmst_curves()}} for a bare
\code{ggplot2} object with one facet per estimator and one step curve per arm.
With \code{tau}, the area under each arm's curve up to \code{tau} is shaded (that
area is the RMST) and a vertical line marks \code{tau}. With \code{estimates}, each
facet is annotated with its RMST difference and interval.
}
\details{
The input follows the \code{tables$curves} contract of
\code{hvtiRpropensity::ps_rmst()}: columns \code{estimator}, \code{arm} (\code{"treated"} or
\code{"control"}), \code{time} and \code{surv}, as right-continuous steps that start at
time 0 with \code{surv} 1. hvtiRpropensity is not required: pass plain data
frames, or the \code{ps_rmst} object itself as \code{curves} and its
\verb{$tables$curves} (and, when \code{estimates} is \code{NULL}, \verb{$tables$estimates}) are
read.
}
\examples{
steps <- function(est, arm, rate) {
  t <- c(0, sort(round(stats::rexp(40, rate) * 365)))
  data.frame(estimator = est, arm = arm, time = t,
             surv = c(1, 1 - seq_len(40) / 41))
}
set.seed(1)
curves <- rbind(
  steps("IPTW", "treated", 1 / 3), steps("IPTW", "control", 1 / 2),
  steps("Overlap", "treated", 1 / 3), steps("Overlap", "control", 1 / 2)
)
est <- data.frame(estimator = c("IPTW", "Overlap"),
                  diff_days = c(41, 35), lo_days = c(-5, 2), hi_days = c(87, 68))

rc <- hv_rmst_curves(curves, estimates = est, tau = 365)
rc

plot(rc) +
  ggplot2::labs(x = "Days", y = "Survival") +
  theme_hv_manuscript()

}
\seealso{
\code{\link[=plot.hv_rmst_curves]{plot.hv_rmst_curves()}}, \code{\link[=hv_rmst_contrast]{hv_rmst_contrast()}}

Other Propensity Score & Matching:
\code{\link[=hv_mirror_hist]{hv_mirror_hist()}},
\code{\link[=hv_rmst_contrast]{hv_rmst_contrast()}},
\code{\link[=plot.hv_mirror_hist]{plot.hv_mirror_hist()}},
\code{\link[=plot.hv_rmst_contrast]{plot.hv_rmst_contrast()}},
\code{\link[=plot.hv_rmst_curves]{plot.hv_rmst_curves()}},
\code{\link[=sample_covariate_balance_data]{sample_covariate_balance_data()}}
}
\concept{Propensity Score & Matching}
