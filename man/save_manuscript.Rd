% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save-manuscript.R
\name{save_manuscript}
\alias{save_manuscript}
\title{Save a ggplot at HVTI manuscript defaults}
\usage{
save_manuscript(
  plot,
  file,
  width = 6,
  height = 4,
  units = "in",
  device = NULL,
  dpi = 300,
  draft_file = NULL,
  draft_dpi = NULL,
  ...
)
}
\arguments{
\item{plot}{A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object, e.g. \verb{plot(hv_*())} finished with
\code{theme_hv_manuscript()}.}

\item{file}{Output file path. Its extension sets the format (\code{.pdf}, \code{.png},
...).}

\item{width, height, units}{Figure size. Default \code{6} x \code{4} \code{"in"} — the HVTI
manuscript default.}

\item{device}{Graphics device. \code{NULL} (default) lets \pkg{ggplot2} pick from
the file extension. For font embedding in a PDF, pass
\code{grDevices::cairo_pdf} (requires cairo/X11 support).}

\item{dpi}{Resolution for raster formats. Default \code{300}.}

\item{draft_file}{Optional second output file path (typically \code{.png}). When
supplied, an additional raster copy of \code{plot} is written here at the same
\code{width}/\code{height}/\code{units}, for dragging into a Word draft manuscript.
Default \code{NULL} (no draft copy written).}

\item{draft_dpi}{Resolution for \code{draft_file}. Default \code{NULL}, which falls
back to \code{dpi}.}

\item{...}{Further arguments passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} for the primary
\code{file}. Not applied to \code{draft_file}.}
}
\value{
Invisibly, the \code{file} path.
}
\description{
A thin wrapper around \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} that enforces the house manuscript
figure size — \strong{6 inches wide by 4 inches tall} — so journal figures come
out at a consistent size in one call. It is the manuscript counterpart of
\code{\link[=save_ppt]{save_ppt()}}, which enforces the slide panel box.
}
\details{
Pair it with \code{\link[=theme_hv_manuscript]{theme_hv_manuscript()}}, whose 12 pt \code{base_size} supplies the
manuscript typography. Like \code{\link[=save_ppt]{save_ppt()}}, \code{save_manuscript()} fixes the
output geometry, not the theme — style the plot first, then save.

The device is inferred from the file extension by default. For a \code{.pdf}
where you want fonts embedded (so 12 pt type renders exactly as designed),
pass \code{device = grDevices::cairo_pdf} — on a system with cairo/X11 support.

Publisher-accepted vector formats (PDF, EPS, TIFF) often produce large or
fragile files when dragged into a Word manuscript — Word has no native
PDF-as-picture support and silently converts them to bloated, sometimes
unreadable EMF. Pass \code{draft_file} (typically a \code{.png} path) to also write a
small raster copy alongside \code{file} in the same call: keep \code{file} as the
vector deliverable actually submitted to the journal, and drag
\code{draft_file} into the Word draft instead.
}
\examples{
\donttest{
p <- plot(hv_survival(sample_survival_data(n = 200, seed = 42))) +
  theme_hv_manuscript()
save_manuscript(p, file.path(tempdir(), "survival.pdf"))

# also write a small draft PNG for dragging into Word
save_manuscript(p, file.path(tempdir(), "survival.pdf"),
                 draft_file = file.path(tempdir(), "survival_draft.png"))
}

}
\seealso{
\code{\link[=save_ppt]{save_ppt()}} for slides, \code{\link[=theme_hv_manuscript]{theme_hv_manuscript()}} for the 12 pt
typography, \code{\link[=hv_ggsave_dims]{hv_ggsave_dims()}} for fixed-panel sizing. Worked examples live
in the HVTI ggplot graphics recipes book,
\url{https://ehrlinger.github.io/hvti_graphics/}.
}
