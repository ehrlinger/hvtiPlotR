% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard-plot.R
\name{sample_hazard_empirical}
\alias{sample_hazard_empirical}
\title{Sample Kaplan-Meier Empirical Points for Hazard Plot Overlay}
\usage{
sample_hazard_empirical(
  n = 500,
  time_max = 10,
  n_bins = 6,
  groups = NULL,
  shape = 1.5,
  scale = 8,
  ci_level = 0.95,
  seed = 42L
)
}
\arguments{
\item{n}{Number of simulated patients. Default \code{500}.}

\item{time_max}{Upper end of the follow-up window (years). Default \code{10}.}

\item{n_bins}{Number of time points at which KM is evaluated. Analogous
to the discrete annotation points in the SAS templates. Default \code{6}.}

\item{groups}{\code{NULL} for a single group, or a named numeric vector of
hazard multipliers matching those passed to \code{\link[=sample_hazard_data]{sample_hazard_data()}}.}

\item{shape}{Weibull shape parameter. Default \code{1.5}.}

\item{scale}{Weibull scale parameter (years). Default \code{8.0}.}

\item{ci_level}{Confidence level. Default \code{0.95}.}

\item{seed}{Random seed. Default \code{42}.}
}
\value{
A data frame with columns \code{time}, \code{estimate}, \code{lower}, \code{upper}, and
(when \code{groups} is not \code{NULL}) \code{group}.
}
\description{
Simulates patient-level survival data from a Weibull distribution and
returns Kaplan-Meier estimates at a small number of binned time points,
matching the structure of the SAS \code{plout} dataset used as an empirical
overlay in \code{tp.hp.dead.sas} and related templates.
}
\details{
\strong{SAS column mapping:}
\itemize{
\item \code{time}     ← \code{IV_DEAD} / \code{iv_dead} (evaluation time points)
\item \code{estimate} ← \code{CUM_SURV} (KM survival estimate, 0–100 \%)
\item \code{lower}    ← \code{CL_LOWER} (lower 95 \% CI)
\item \code{upper}    ← \code{CL_UPPER} (upper 95 \% CI)
\item \code{group}    ← stratification variable (when \code{groups} is not \code{NULL})
}
}
\examples{
emp <- sample_hazard_empirical(n = 500, time_max = 10, n_bins = 6)
head(emp)

emp2 <- sample_hazard_empirical(
  n = 400, time_max = 10,
  groups = c("No Takedown" = 1.0, "Takedown" = 0.65)
)
head(emp2)
}
\seealso{
\code{\link[=hv_hazard]{hv_hazard()}}, \code{\link[=sample_hazard_data]{sample_hazard_data()}}
}
