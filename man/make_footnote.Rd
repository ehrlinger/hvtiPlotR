% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdf_footnote.R
\name{make_footnote}
\alias{make_footnote}
\alias{makeFootnote}
\title{Add a Draft Footnote to a Figure}
\usage{
make_footnote(
  text = getwd(),
  timestamp = TRUE,
  prefix = "DRAFT — ",
  size = 0.7,
  colour = grey(0.5),
  x = 1,
  y = 0,
  hjust = "right",
  vjust = "bottom",
  margin_mm = 2
)

makeFootnote(
  footnoteText = getwd(),
  size = 0.7,
  color = grey(0.5),
  timestamp = TRUE
)
}
\arguments{
\item{text}{Text to display. Defaults to the current working directory,
which conveniently identifies the project. For the typical use case pass
the script filename: \code{make_footnote("R/analysis.R")}.}

\item{timestamp}{Logical; append \code{Sys.time()} to \code{text}? Default \code{TRUE}.
Set to \code{FALSE} for reproducible screenshots or when the file path already
contains enough context.}

\item{prefix}{String prepended to \code{text} before the timestamp. Default
\code{"DRAFT \\u2014 "}. Set to \code{""} to suppress the prefix.}

\item{size}{Font size as a multiplier relative to the device default
(passed to \code{\link[grid:gpar]{grid::gpar()}} as \code{cex}). Default \code{0.7}.}

\item{colour}{Font colour. Default \code{grey(0.5)} (medium grey), which is
visually unobtrusive on both screen and print.}

\item{x}{Horizontal position in normalised parent coordinates
(\code{"npc"}). Default \code{1} (right edge). Decrease to move left.}

\item{y}{Vertical position in \code{"npc"}. Default \code{0} (bottom). Increase
to move up.}

\item{hjust}{Horizontal justification: \code{"right"} (default), \code{"left"},
or \code{"centre"}.}

\item{vjust}{Vertical justification: \code{"bottom"} (default), \code{"top"},
or \code{"centre"}.}

\item{margin_mm}{Margin in mm pulled back from the \code{x}/\code{y} position.
Default \code{2}.}

\item{footnoteText}{Equivalent to \code{text} in \code{\link[=make_footnote]{make_footnote()}}.}

\item{color}{Equivalent to \code{colour} in \code{\link[=make_footnote]{make_footnote()}}.}
}
\value{
Called for its side effect (draws text on the current device).
Returns \code{invisible(NULL)}.
}
\description{
Writes a small text annotation in the bottom-right corner of the \strong{current}
graphics device using grid. Call this \emph{after} printing or displaying the
plot. For publication-ready figures, simply omit the call — the plot is
unchanged.
}
\details{
\strong{Typical workflow:}

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# During analysis (draft)
p <- plot(hv_hazard(...)) + theme_hv_manuscript()
print(p)
make_footnote("analysis/mortality.R")   # adds draft annotation

# For publication — just don't call make_footnote()
ggsave("figures/fig1.pdf", p, width = 11, height = 8.5)
}\if{html}{\out{</div>}}
}
\examples{
# --- Basic use after a base-R plot ----------------------------------------
plot(1:10, main = "Example")
make_footnote("examples/basic.R")

# --- With a ggplot2 figure and manuscript theme ---------------------------
\dontrun{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg)) +
  geom_point() +
  labs(title = "Motor Trend Cars") +
  theme_hv_manuscript()

# Draft: print, then annotate
print(p)
make_footnote("R/exploratory.R")

# Publication: save without footnote
ggsave("figures/fig1.pdf", p, width = 11, height = 8.5)
}

# --- Custom position and no timestamp ------------------------------------
plot(1:10)
make_footnote("Preliminary results", timestamp = FALSE, prefix = "")

# --- Suppress the DRAFT prefix -------------------------------------------
plot(1:10)
make_footnote(
  text      = paste("Analyst: J. Ehrlinger |", Sys.Date()),
  timestamp = FALSE,
  prefix    = ""
)

}
\seealso{
\code{\link[=save_ppt]{save_ppt()}}, \code{\link[=theme_hv_manuscript]{theme_hv_manuscript()}}
}
