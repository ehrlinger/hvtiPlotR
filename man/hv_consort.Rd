% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consort-plot.R
\name{hv_consort}
\alias{hv_consort}
\title{Build a CONSORT flow diagram from a tracker}
\usage{
hv_consort(
  tracker,
  side_box = "all",
  cex = 0.9,
  width = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{tracker}{An \code{hv_consort_tracker} with at least two stages (call
\code{\link[=hv_consort_exclude]{hv_consort_exclude()}} at least once after \code{\link[=hv_consort_start]{hv_consort_start()}}).}

\item{side_box}{Character vector of exclusion-reason column names to display
as side boxes, or \code{"all"} (default) to include every exclusion column.}

\item{cex}{Numeric; text size scaling passed to \code{consort::consort_plot()}.
Default \code{0.9}.}

\item{width}{Diagram width in inches.  Defaults to \code{7}.}

\item{height}{Diagram height in inches.  Defaults to \code{2 + n_stages * 1.2},
where \code{n_stages} is the number of stages in the tracker.}

\item{...}{Additional arguments forwarded to \code{consort::consort_plot()}.}
}
\value{
An \code{hv_consort} object -- a list with:
\describe{
\item{\verb{$plot}}{The grid object returned by \code{consort::consort_plot()}.}
\item{\verb{$meta}}{Named list: \code{n_stages}, \code{width}, \code{height}, \code{orders}, \code{side_box}.}
\item{\verb{$tracker}}{The original \code{hv_consort_tracker}.}
}
}
\description{
Reads the stage metadata stored in an \code{hv_consort_tracker}, auto-derives
the \code{orders} and \code{side_box} arguments for \code{consort::consort_plot()}, and
returns an \code{hv_consort} object wrapping the grid diagram.
}
\examples{
cohort <- data.frame(
  mrn  = paste0("P", 1:100),
  age  = sample(15:80, 100, TRUE),
  echo = sample(c(TRUE, FALSE), 100, TRUE, prob = c(0.9, 0.1))
)
tracker <- hv_consort_start(cohort, patient_id = mrn) |>
  hv_consort_exclude(label = "Eligible", col = "excl_screen",
                      age < 18 ~ "Age < 18") |>
  hv_consort_exclude(label = "Analyzed", col = "excl_eligible",
                      !echo ~ "Missing echocardiogram")
fig <- hv_consort(tracker)
\dontrun{plot(fig)}

}
\seealso{
Worked recipe with rendered output:
\url{https://ehrlinger.github.io/hvti_graphics/consort.html}.

\code{\link[=hv_consort_start]{hv_consort_start()}}, \code{\link[=hv_consort_exclude]{hv_consort_exclude()}}, \code{\link[=plot.hv_consort]{plot.hv_consort()}}
}
