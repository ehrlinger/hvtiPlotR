% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster-sankey-plot.R
\name{cluster_sankey_plot}
\alias{cluster_sankey_plot}
\title{Cluster Stability Sankey Plot}
\usage{
cluster_sankey_plot(
  data,
  cluster_cols = paste0("C", 2:9),
  node_levels = NULL,
  node_colours = NULL,
  alpha = 0.8,
  label_size = 8,
  label_hjust = -0.05
)
}
\arguments{
\item{data}{Data frame; one row per patient. Must contain all
columns named in \code{cluster_cols}.}

\item{cluster_cols}{Character vector of column names giving the cluster
assignments at each value of K, in ascending K order. Default
\code{paste0("C", 2:9)} (columns \code{C2}, \code{C3}, …, \code{C9}).}

\item{node_levels}{Character vector giving the display order of node
labels (bottom to top within each column). If \code{NULL} (default), the
existing factor levels of the first cluster column are used; if that
column is not a factor, levels are taken in order of first appearance.}

\item{node_colours}{Named character vector mapping node labels to fill
colours. If \code{NULL} (default), colours are drawn from
\code{RColorBrewer::brewer.pal(9, "Set1")} in the order
\code{c(2, 6, 8, 4, 3, 5, 7, 1, 9)} matching the original analysis.
Truncated or extended automatically to the number of unique nodes.}

\item{alpha}{Transparency applied to flow bands and node labels.
Default \code{0.8}.}

\item{label_size}{Font size for node labels in points. Default \code{8}.}

\item{label_hjust}{Horizontal justification offset for node labels.
Negative values shift labels to the right of the node centre. Default
\code{-0.05}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object using \code{ggsankey} geoms. Compose with
\code{scale_fill_manual()}, \code{labs()}, \code{theme()}, and \code{\link[=hvti_theme]{hvti_theme()}}.
}
\description{
Draws a Sankey diagram showing how patients flow between labelled clusters
as the number of clusters K increases. Each column represents one value of
K; each band shows the fraction of patients whose cluster assignment changes
between K and K+1. Node labels show the cluster letter and patient count.
}
\details{
This ports the PAM cluster stability figure produced by the HVTI clustering
analysis pipeline (source code using \code{ggsankey::make_long()},
\code{geom_sankey()}, and \code{geom_sankey_label()}).

\strong{Requires the \code{ggsankey} package.} Install with:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{remotes::install_github("davidsjoberg/ggsankey")
}\if{html}{\out{</div>}}
}
\examples{
dta <- sample_cluster_sankey_data(n = 300, seed = 42)
head(dta)
table(dta$C9)

# cluster_sankey_plot() requires ggsankey (GitHub-only package).
# Install with: remotes::install_github("davidsjoberg/ggsankey")
if (requireNamespace("ggsankey", quietly = TRUE)) {

  # --- Default colours (Set1 palette) -----------------------------------
  cluster_sankey_plot(dta) +
    ggplot2::labs(x = NULL, title = "Cluster Stability: K = 2 to 9") +
    hvti_theme("manuscript")

  # --- Custom colour palette --------------------------------------------
  my_cols <- c(
    A = "#1f77b4", B = "#ff7f0e", C = "#2ca02c", D = "#d62728",
    E = "#9467bd", F = "#8c564b", G = "#e377c2", H = "#7f7f7f",
    I = "#bcbd22"
  )
  cluster_sankey_plot(dta, node_colours = my_cols) +
    ggplot2::labs(x = NULL) +
    hvti_theme("manuscript")

  # --- Subset to K = 2 to 6 only ----------------------------------------
  cluster_sankey_plot(dta, cluster_cols = paste0("C", 2:6)) +
    ggplot2::labs(x = NULL) +
    hvti_theme("manuscript")
}

}
\seealso{
\code{\link[=sample_cluster_sankey_data]{sample_cluster_sankey_data()}}, \code{\link[=hvti_theme]{hvti_theme()}}
}
