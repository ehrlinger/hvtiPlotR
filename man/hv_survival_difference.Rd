% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard-plot.R
\name{hv_survival_difference}
\alias{hv_survival_difference}
\title{Prepare survival difference (life-gained) data for plotting}
\usage{
hv_survival_difference(
  diff_data,
  x_col = "time",
  estimate_col = "difference",
  lower_col = NULL,
  upper_col = NULL,
  group_col = NULL
)
}
\arguments{
\item{diff_data}{Data frame of pre-computed survival differences.
See \code{\link[=sample_survival_difference_data]{sample_survival_difference_data()}}.}

\item{x_col}{Name of the time column. Default \code{"time"}.}

\item{estimate_col}{Name of the difference column. Default \code{"difference"}.}

\item{lower_col}{Lower CI column, or \code{NULL}. Default \code{NULL}.}

\item{upper_col}{Upper CI column, or \code{NULL}. Default \code{NULL}.}

\item{group_col}{Grouping column for multiple comparisons, or \code{NULL}.
Default \code{NULL}.}
}
\value{
An S3 object of class \code{c("hv_survival_difference", "hv_data")}.
}
\description{
Stores pre-computed survival difference data as an
\code{hv_survival_difference} object.  Pass the result to
\code{\link[=plot.hv_survival_difference]{plot.hv_survival_difference()}} to render the plot.  Covers
\code{tp.hp.dead.life-gained.sas} and the survival-difference component of
\code{tp.hp.numtreat.survdiff.matched.sas}.
}
\examples{
library(ggplot2)

diff_dat <- sample_survival_difference_data(
  groups = c("Control" = 1.0, "Treatment" = 0.70)
)

sd <- hv_survival_difference(diff_dat,
  lower_col = "diff_lower", upper_col = "diff_upper"
)
plot(sd) +
  geom_hline(yintercept = 0, linetype = "dashed", colour = "grey50") +
  scale_x_continuous(limits = c(0, 10), breaks = 0:10) +
  scale_y_continuous(limits = c(-5, 30),
                     labels = function(x) paste0(x, "\%")) +
  labs(x = "Years", y = "Survival Difference (\%)") +
  hv_theme("manuscript")

}
\seealso{
\code{\link[=plot.hv_survival_difference]{plot.hv_survival_difference()}},
\code{\link[=sample_survival_difference_data]{sample_survival_difference_data()}}, \code{\link[=hv_nnt]{hv_nnt()}}
}
