% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hvti-data.R
\name{autoplot.hv_data}
\alias{autoplot.hv_data}
\title{Produce a ggplot from an hv_data object (\code{autoplot} generic)}
\usage{
\method{autoplot}{hv_data}(object, ...)
}
\arguments{
\item{object}{An \code{hv_data} object.}

\item{...}{Forwarded to the subclass's \code{plot()} method.}
}
\value{
A \code{ggplot} object (or a \code{patchwork} composite when the underlying
\code{plot()} method returns one, e.g. for \code{hv_upset}).
}
\description{
Re-exports ggplot2's \code{\link[ggplot2:autoplot]{ggplot2::autoplot()}} for \code{hv_data} objects so callers
who prefer the ggplot2-ecosystem verb (\code{broom} / \code{ggfortify} / \code{ggsurvfit}
all use it) can write \code{autoplot(km)} interchangeably with \code{plot(km)}.
}
\details{
Dispatches to the registered \verb{plot.<subclass>()} method, so any args
that work with \code{plot()} work with \code{autoplot()}.
}
