% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/goodness-followup.R
\name{goodness_event_plot}
\alias{goodness_event_plot}
\title{Goodness of Follow-Up — Event Panel}
\usage{
goodness_event_plot(
  data,
  event_col,
  event_time_col,
  death_for_event_col = NULL,
  event_levels = c("No event", "Non-fatal event", "Death"),
  iv_opyrs_col = "iv_opyrs",
  death_col = "dead",
  origin_year = 1990,
  study_start = as.Date("1990-01-01"),
  study_end = as.Date("2019-12-31"),
  close_date = as.Date("2021-08-06"),
  tolower_names = TRUE,
  alpha = 0.8,
  segment_drop = 0.2,
  diagonal_color = "orange",
  diagonal_linetype = "dashed",
  diagonal_linewidth = 0.6
)
}
\arguments{
\item{data}{Data frame. See \code{\link[=sample_goodness_followup_data]{sample_goodness_followup_data()}}.}

\item{event_col}{Name of the non-fatal event indicator column
(required, no default).}

\item{event_time_col}{Name of the time-to-event column (required, no
default).}

\item{death_for_event_col}{Name of the death column to use for the event
panel, or \code{NULL} to use the default \code{death_col}. Default \code{NULL}.}

\item{event_levels}{Character vector of exactly three labels:
event-free, non-fatal event, death. Default
\code{c("No event", "Non-fatal event", "Death")}.}

\item{iv_opyrs_col}{Column name holding the numeric interval (in years) from
\code{origin_year} to the operation date.}

\item{death_col}{Logical (or coercible) indicator for death. Used for the
death panel and, by default, for the death component of the event panel.}

\item{origin_year}{Reference calendar year that matches zero in
\code{iv_opyrs_col}.}

\item{study_start, study_end, close_date}{Dates that define the diagonal
potential follow-up line.}

\item{tolower_names}{When \code{TRUE}, column names are converted to lower case
prior to processing.}

\item{alpha}{Transparency passed to the point and segment layers.}

\item{segment_drop}{Amount (in years) subtracted from each follow-up value
to draw the short vertical tick beneath each point.}

\item{diagonal_color}{Color of the potential follow-up reference line.
Default \code{"orange"}.}

\item{diagonal_linetype}{Line type of the reference line. Default
\code{"dashed"}.}

\item{diagonal_linewidth}{Line width of the reference line. Default \code{0.6}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object (event panel).
}
\description{
Produces the event follow-up panel: each patient is plotted at their
operation date (x) and follow-up time (y), with a three-level \code{state}
factor driving colour and shape: event-free, non-fatal event, or death.
Compose with \code{scale_color_manual()}, \code{scale_shape_manual()}, \code{labs()},
and \code{\link[=hvti_theme]{hvti_theme()}}.
}
\examples{
dta <- sample_goodness_followup_data()

goodness_event_plot(
  dta,
  event_col      = "ev_event",
  event_time_col = "iv_event"
) +
  ggplot2::scale_color_manual(
    values = c("No event" = "blue", "Non-fatal event" = "green3",
               "Death" = "red"),
    name = NULL
  ) +
  ggplot2::scale_shape_manual(values = c(1, 2, 4), name = NULL) +
  ggplot2::labs(x = "Operation Date", y = "Follow-up (years)") +
  hvti_theme("manuscript")

}
\seealso{
\code{\link[=goodness_followup]{goodness_followup()}}, \code{\link[=sample_goodness_followup_data]{sample_goodness_followup_data()}}
}
