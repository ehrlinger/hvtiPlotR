% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonparametric-ordinal-plot.R
\name{sample_nonparametric_ordinal_data}
\alias{sample_nonparametric_ordinal_data}
\title{Sample Nonparametric Ordinal Curve Data}
\usage{
sample_nonparametric_ordinal_data(
  n = 1000,
  time_max = 5,
  n_points = 500,
  grade_labels = c("Grade 0", "Grade 1", "Grade 2", "Grade 3"),
  n_bins = 10,
  seed = 42L
)
}
\arguments{
\item{n}{Number of simulated patients (controls CI width and
data-point variability). Default \code{1000}.}

\item{time_max}{Upper end of the time axis (years). Default \code{5}.}

\item{n_points}{Number of time points on the prediction grid. Default
\code{500}.}

\item{grade_labels}{Character vector of grade labels, one per grade level
in ascending order. Default
\code{c("Grade 0", "Grade 1", "Grade 2", "Grade 3")}.}

\item{n_bins}{Number of equal-sized bins for the data summary points.
Default \code{10}.}

\item{seed}{Random seed. Default \code{42}.}
}
\value{
A long-format data frame: \code{time}, \code{estimate}, \code{grade} (factor).
Individual grade probabilities sum to 1 at each time point.
}
\description{
Simulates pre-computed grade-specific probability curves matching what SAS
produces after fitting a cumulative proportional-odds nonparametric temporal
trend model. Covers the ordinal TR / AR grade patterns from:
\itemize{
\item \code{tp.np.tr.ivecho.average_curv.ordinal.sas} (p0, p1, p2, p3 individual probs)
\item \code{tp.np.po_ar.u_multi.ordinal.sas} (multi-scenario ordinal, p34 = p3+p4)
}
}
\details{
\strong{SAS context:} The SAS \code{predict} dataset has one column per grade
(\code{p0}, \code{p1}, \code{p2}, \code{p3}) after computing individual probabilities from
cumulative probabilities (\code{cp0 = co0/(1+co0)}, etc.). Export it to CSV
and reshape to long format to use your own model output.
}
\examples{
dat <- sample_nonparametric_ordinal_data(n = 800, time_max = 5)
head(dat)
# verify probabilities sum to 1 at each time point
tapply(dat$estimate[dat$time == dat$time[1]],
       dat$grade[dat$time == dat$time[1]], sum)
}
\seealso{
\code{\link[=hvti_ordinal]{hvti_ordinal()}}, \code{\link[=sample_nonparametric_curve_data]{sample_nonparametric_curve_data()}},
\code{\link[=sample_nonparametric_ordinal_points]{sample_nonparametric_ordinal_points()}}
}
