% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariate-balance.R
\name{plot.hv_balance}
\alias{plot.hv_balance}
\title{Plot an hv_balance object}
\usage{
\method{plot}{hv_balance}(
  x,
  point_size = 3,
  alpha = 0.8,
  hline_linetype = "dashed",
  hline_linewidth = 0.25,
  vline_linewidth = 0.2,
  threshold_linetype = "dotted",
  ...
)
}
\arguments{
\item{x}{An \code{hv_balance} object.}

\item{point_size}{Passed to \code{geom_point()}. Default \code{3}.}

\item{alpha}{Point transparency in \eqn{[0,1]}. Default \code{0.8}.}

\item{hline_linetype}{Linetype for horizontal covariate guides.
Default \code{"dashed"}.}

\item{hline_linewidth}{Linewidth for horizontal guides. Default \code{0.25}.}

\item{vline_linewidth}{Linewidth for the zero reference line.
Default \code{0.2}.}

\item{threshold_linetype}{Linetype for the \eqn{\pm}threshold lines.
Default \code{"dotted"}.}

\item{...}{Ignored; present for S3 consistency.}
}
\value{
A bare \code{\link[ggplot2]{ggplot}} object.
}
\description{
Builds a bare covariate balance \code{ggplot2} object from an
\code{\link{hv_balance}} data object.  Each covariate appears as a
labelled row; points show the standardized mean difference per group.
A solid line marks zero; dotted lines mark \eqn{\pm}\code{threshold}.
Add colour, shape, axis scales, and a theme with \code{+}.
}
\examples{
dta <- sample_covariate_balance_data()
cb  <- hv_balance(dta)

plot(cb) +
  ggplot2::scale_color_manual(
    values = c("Before match" = "red4", "After match" = "blue3"),
    name   = NULL
  ) +
  ggplot2::labs(x = "Standardized difference (\%)", y = "") +
  theme_hv_poster()

}
\seealso{
\code{\link{hv_balance}}, \code{\link{theme_hv_manuscript}}
}
