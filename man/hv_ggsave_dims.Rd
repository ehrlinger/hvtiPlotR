% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggsave-dims.R
\name{hv_ggsave_dims}
\alias{hv_ggsave_dims}
\title{Compute \code{ggsave()} dimensions for a fixed panel content area}
\usage{
hv_ggsave_dims(plot, width, height, units = c("in", "cm", "mm"))
}
\arguments{
\item{plot}{A ggplot (or patchwork) object.}

\item{width}{Target panel content area width, in \code{units}.}

\item{height}{Target panel content area height, in \code{units}.}

\item{units}{One of \code{"in"}, \code{"cm"}, \code{"mm"}. Default \code{"in"}.}
}
\value{
A named list with elements \code{width}, \code{height}, \code{units} — shaped to
splat directly into \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} via \code{\link[=do.call]{do.call()}} (see examples).
}
\description{
Given a fitted ggplot, returns \code{width} and \code{height} values such that, when
passed to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}, the resulting file has a panel content area
matching \code{width} / \code{height}. The "panel content area" is the bounding box
that contains all panels and their internal strips and gutters, but
excludes axes, axis titles, plot title, caption, legend, and plot margins.
}
\details{
Useful for multi-panel figure sets where a constant data region is
required across PDFs regardless of label length or legend placement.
Always measured with a PDF sizing device, which is why \code{units} is limited
to length (inches, cm, mm) — DPI is irrelevant for vector output.
}
\examples{
\dontrun{
p <- ggplot2::ggplot(mtcars, ggplot2::aes(hp, mpg)) +
  ggplot2::geom_point() +
  ggplot2::labs(title = "Long title that eats vertical space",
                x = "Horsepower", y = "Miles per gallon")

dims <- hv_ggsave_dims(p, width = 4, height = 3)
do.call(
  ggplot2::ggsave,
  c(list(filename = "fig.pdf", plot = p), dims)
)
}
}
