% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmst-plots.R
\name{hv_rmst_contrast}
\alias{hv_rmst_contrast}
\title{Prepare RMST contrast data for plotting}
\usage{
hv_rmst_contrast(
  data,
  estimator = "estimator",
  diff = "diff_days",
  lo = "lo_days",
  hi = "hi_days",
  ...
)
}
\arguments{
\item{data}{A data frame with one row per estimator, or a \code{ps_rmst}
object (any list whose \verb{$tables$estimates} is a data frame).}

\item{estimator}{Name of the column holding estimator labels. Default
\code{"estimator"}. If it is a factor, its levels set the display order;
otherwise the order of first appearance is used.}

\item{diff}{Name of the numeric column with the RMST difference (in
days). Default \code{"diff_days"}.}

\item{lo, hi}{Names of the numeric columns with the lower and upper
interval limits (in days). Defaults \code{"lo_days"} and \code{"hi_days"}.}

\item{...}{Ignored; present for S3 consistency.}
}
\value{
An object of class \code{c("hv_rmst_contrast", "hv_data")}: a list with
\describe{
\item{\verb{$data}}{The estimates data frame, with the estimator column
as supplied.}
\item{\verb{$meta}}{Named list: \code{estimator}, \code{diff}, \code{lo}, \code{hi} (column
names), \code{estimator_levels}, \code{n_estimators}, \code{n_missing}.}
\item{\verb{$tables}}{Empty list.}
}
}
\description{
Validates a table of restricted mean survival time (RMST) differences, one
row per weighting estimator, and returns an \code{hv_rmst_contrast} object.
Call \code{\link[=plot.hv_rmst_contrast]{plot.hv_rmst_contrast()}} on the result for a bare dot-and-whisker
\code{ggplot2} object: one point per estimator, a whisker for its interval, and a
vertical reference line at zero.
}
\details{
The input follows the \code{tables$estimates} contract of
\code{hvtiRpropensity::ps_rmst()}: columns \code{estimator}, \code{subset}, \code{weighting},
\code{n}, \code{ess_treated}, \code{ess_control}, \code{rmst_treated}, \code{rmst_control}, \code{diff},
\code{diff_days}, \code{lo_days}, \code{hi_days} and \code{n_failed}, where a positive \code{diff}
favours the treated arm. Only the estimator, difference and interval
columns are used here. hvtiRpropensity is not required: pass a plain data
frame, or the \code{ps_rmst} object itself and its \verb{$tables$estimates} is read.
}
\examples{
est <- data.frame(
  estimator = factor(c("IPTW", "Overlap", "Matching"),
                     levels = c("IPTW", "Overlap", "Matching")),
  diff_days = c(41, 35, 28),
  lo_days   = c(-5, 2, -14),
  hi_days   = c(87, 68, 70)
)
rc <- hv_rmst_contrast(est)
rc

plot(rc) +
  ggplot2::labs(y = NULL) +
  theme_hv_manuscript()

}
\seealso{
\code{\link[=plot.hv_rmst_contrast]{plot.hv_rmst_contrast()}}, \code{\link[=hv_rmst_curves]{hv_rmst_curves()}}

Other Propensity Score & Matching:
\code{\link[=hv_mirror_hist]{hv_mirror_hist()}},
\code{\link[=hv_rmst_curves]{hv_rmst_curves()}},
\code{\link[=plot.hv_mirror_hist]{plot.hv_mirror_hist()}},
\code{\link[=plot.hv_rmst_contrast]{plot.hv_rmst_contrast()}},
\code{\link[=plot.hv_rmst_curves]{plot.hv_rmst_curves()}},
\code{\link[=sample_covariate_balance_data]{sample_covariate_balance_data()}}
}
\concept{Propensity Score & Matching}
