% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spaghetti-plot.R
\name{sample_spaghetti_data}
\alias{sample_spaghetti_data}
\title{Sample Spaghetti / Profile Plot Data}
\usage{
sample_spaghetti_data(
  n_patients = 150,
  max_obs = 6,
  groups = c(Female = 0.45, Male = 0.55),
  seed = 42L
)
}
\arguments{
\item{n_patients}{Number of unique patients. Default \code{150}.}

\item{max_obs}{Maximum number of observations per patient. Default \code{6}.}

\item{groups}{Named character vector of group labels and their
sampling probabilities (summing to 1). Default
\code{c(Female = 0.45, Male = 0.55)}.}

\item{seed}{Random seed for reproducibility. Default \code{42}.}
}
\value{
A data frame with columns:
\itemize{
\item \code{id}    — patient identifier (integer)
\item \code{time}  — years from index procedure (numeric)
\item \code{value} — continuous outcome (numeric; AV mean gradient in mmHg)
\item \code{group} — group label (factor)
}
}
\description{
Generates a realistic repeated-measures longitudinal data set for
demonstrating \code{\link[=hv_spaghetti]{hv_spaghetti()}}. Each row is one observation for one
patient at one time point, mimicking serial echocardiographic measurements
after cardiac surgery (AV mean gradient trajectory over follow-up).
Patients have an irregular number of follow-up measurements and a group
indicator (e.g. sex), matching the \code{b_echo.xpt} structure in the template.
}
\examples{
dta <- sample_spaghetti_data(n_patients = 100, seed = 42)
head(dta)
table(dta$group)
}
\seealso{
\code{\link[=hv_spaghetti]{hv_spaghetti()}}
}
