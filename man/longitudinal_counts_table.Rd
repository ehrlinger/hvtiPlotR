% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/longitudinal-counts-plot.R
\name{longitudinal_counts_table}
\alias{longitudinal_counts_table}
\title{Longitudinal Participation Counts Table Panel}
\usage{
longitudinal_counts_table(
  data,
  x_col = "time_label",
  count_col = "count",
  group_col = "series",
  label_format = NULL
)
}
\arguments{
\item{data}{Long-format data frame. See \code{\link[=sample_longitudinal_counts_data]{sample_longitudinal_counts_data()}}.}

\item{x_col}{Name of the discrete time-label column. Default \code{"time_label"}.}

\item{count_col}{Name of the numeric count column. Default \code{"count"}.}

\item{group_col}{Name of the series grouping column. Default \code{"series"}.}

\item{label_format}{Formatting function applied to count values.
\code{NULL} (default) auto-selects: uses \link[scales:comma]{scales::comma} when the \code{scales}
package is installed, otherwise falls back to \link[base:character]{base::as.character}.
Pass \code{identity} to display counts with no formatting.}
}
\value{
A bare \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object (text table panel).
}
\description{
Produces a numeric data table rendered as a ggplot text panel, intended to
be composed below \code{\link[=longitudinal_counts_plot]{longitudinal_counts_plot()}} via \code{patchwork}.
}
\examples{
library(ggplot2)
dta <- sample_longitudinal_counts_data(n_patients = 300, seed = 42L)

longitudinal_counts_table(dta) +
  scale_colour_manual(
    values = c(Patients = "steelblue", Measurements = "firebrick"),
    guide  = "none"
  ) +
  hvti_theme("manuscript")

}
\seealso{
\code{\link[=longitudinal_counts_plot]{longitudinal_counts_plot()}}, \code{\link[=sample_longitudinal_counts_data]{sample_longitudinal_counts_data()}}
}
