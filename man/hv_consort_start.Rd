% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consort-plot.R
\name{hv_consort_start}
\alias{hv_consort_start}
\title{Initialise a CONSORT patient-flow tracker}
\usage{
hv_consort_start(data, patient_id, label = "Screened", pass_col = NULL)
}
\arguments{
\item{data}{A data frame — one row per patient.}

\item{patient_id}{<\code{\link[rlang:args_data_masking]{data-masking}}> Unquoted
name of the unique patient identifier column.}

\item{label}{Character label for the initial population box.
Default \code{"Screened"}.}

\item{pass_col}{Column name for the initial boolean column.  Defaults to
\code{ct_snakify(label)} (e.g. \code{"screened"} when \code{label = "Screened"}).}
}
\value{
An \code{hv_consort_tracker} object — a list with:
\describe{
\item{\verb{$data}}{Patient-level data frame with boolean/character columns appended per stage.}
\item{\verb{$stages}}{Ordered list of stage descriptors (\code{label}, \code{include_col}, \code{excl_col}, \code{excl_label}).}
\item{\verb{$patient_id_col}}{Column name of the patient identifier.}
}
}
\description{
Creates an \code{hv_consort_tracker} object with one row per patient and a
boolean column indicating that every patient is in the initial (screened)
population.  Build the tracker incrementally with \code{\link[=hv_consort_exclude]{hv_consort_exclude()}},
then convert to a diagram with \code{\link[=hv_consort]{hv_consort()}}.
}
\examples{
cohort  <- data.frame(mrn = paste0("P", 1:100), age = sample(15:80, 100, TRUE))
tracker <- hv_consort_start(cohort, patient_id = mrn)
print(tracker)

}
\seealso{
\code{\link[=hv_consort_exclude]{hv_consort_exclude()}}, \code{\link[=hv_consort]{hv_consort()}}
}
