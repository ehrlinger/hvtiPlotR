% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ph-location.R
\name{hv_ph_location}
\alias{hv_ph_location}
\title{Compute \code{officer::ph_location()} args for a fixed-panel slide layout}
\usage{
hv_ph_location(
  plot,
  panel_width,
  panel_height,
  panel_left,
  panel_top,
  units = c("in", "cm", "mm")
)
}
\arguments{
\item{plot}{A ggplot (or patchwork) object.}

\item{panel_width}{Target panel width, in \code{units}.}

\item{panel_height}{Target panel height, in \code{units}.}

\item{panel_left}{Distance from left edge of slide to the left edge of
the panel, in \code{units}.}

\item{panel_top}{Distance from top edge of slide to the top edge of
the panel, in \code{units}.}

\item{units}{One of \code{"in"}, \code{"cm"}, \code{"mm"}. Default \code{"in"}.}
}
\value{
A named list with elements \code{width}, \code{height}, \code{left}, \code{top} — all
in \code{units}. Splat into \code{\link[officer:ph_location]{officer::ph_location()}} via \code{\link[=do.call]{do.call()}}.
}
\description{
Given a fitted ggplot and a target panel rectangle (width/height/left/top
on the slide), returns the \code{width}, \code{height}, \code{left}, \code{top} values to pass
to \code{\link[officer:ph_location]{officer::ph_location()}} such that the \emph{panel content area} of the plot
lands at the specified slide coordinates — regardless of how much room the
axis labels, axis titles, legend, plot title, or plot margins consume.
}
\details{
The panel content area is the rectangular bounding box of the gtable cells
tagged \code{panel}. Strip grobs that sit inside that bounding box are part of
the target; strips outside it (e.g. \code{facet_grid} side strips, the strip
row above a single-row \code{facet_wrap}) are treated as chrome.

Use with \code{\link[=save_ppt]{save_ppt()}} via \code{panel_box = list(width, height, left, top)} to
apply this to every slide in a deck.
}
\examples{
\dontrun{
p <- ggplot2::ggplot(mtcars, ggplot2::aes(hp, mpg)) +
  ggplot2::geom_point() +
  theme_hv_ppt_dark()

loc <- hv_ph_location(
  p,
  panel_width  = 10, panel_height = 5,
  panel_left   = 0.5, panel_top   = 1.5
)
# doc <- officer::read_pptx(template)
# doc <- officer::add_slide(doc, layout = "Title and Content")
# doc <- officer::ph_with(
#   doc, rvg::dml(ggobj = p),
#   location = do.call(officer::ph_location, loc)
# )
}

}
\seealso{
\code{\link[=hv_ggsave_dims]{hv_ggsave_dims()}} for the sizing-only analogue used with
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}, and \code{\link[=save_ppt]{save_ppt()}} which accepts a \code{panel_box}
argument that delegates to this helper.
}
