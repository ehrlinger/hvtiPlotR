% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rmst-plots.R
\name{plot.hv_rmst_curves}
\alias{plot.hv_rmst_curves}
\title{Plot an hv_rmst_curves object}
\usage{
\method{plot}{hv_rmst_curves}(
  x,
  shade_alpha = 0.2,
  linewidth = 0.6,
  tau_linetype = "dashed",
  label_size = 3,
  ...
)
}
\arguments{
\item{x}{An \code{hv_rmst_curves} object.}

\item{shade_alpha}{Transparency of the shaded area. Default \code{0.2}.}

\item{linewidth}{Step line width. Default \code{0.6}.}

\item{tau_linetype}{Linetype of the vertical \code{tau} line. Default
\code{"dashed"}.}

\item{label_size}{Text size of the facet annotations. Default \code{3}.}

\item{...}{Ignored; present for S3 consistency.}
}
\value{
A bare \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
Builds a bare \code{ggplot2} object from an \code{\link[=hv_rmst_curves]{hv_rmst_curves()}} object: weighted
Kaplan-Meier step curves, one facet per estimator, coloured by arm. When
\code{tau} was supplied, the area under each curve up to \code{tau} is shaded
(\code{geom_ribbon()} over a staircase polygon, so the shading follows the steps
exactly) and a vertical line marks \code{tau}. When \code{estimates} was supplied, the
top right of each facet carries the RMST difference and interval. Colour and
fill both map to arm: set them together with \verb{scale_colour_*()} and
\verb{scale_fill_*()}.
}
\examples{
curves <- data.frame(
  estimator = rep(c("IPTW", "Overlap"), each = 6),
  arm       = rep(rep(c("treated", "control"), each = 3), 2),
  time      = rep(c(0, 200, 500), 4),
  surv      = c(1, .9, .7, 1, .8, .5, 1, .92, .75, 1, .85, .6)
)
plot(hv_rmst_curves(curves, tau = 365), shade_alpha = 0.3)

}
\seealso{
\code{\link[=hv_rmst_curves]{hv_rmst_curves()}}

Other Propensity Score & Matching:
\code{\link[=hv_mirror_hist]{hv_mirror_hist()}},
\code{\link[=hv_rmst_contrast]{hv_rmst_contrast()}},
\code{\link[=hv_rmst_curves]{hv_rmst_curves()}},
\code{\link[=plot.hv_mirror_hist]{plot.hv_mirror_hist()}},
\code{\link[=plot.hv_rmst_contrast]{plot.hv_rmst_contrast()}},
\code{\link[=sample_covariate_balance_data]{sample_covariate_balance_data()}}
}
\concept{Propensity Score & Matching}
