% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard-plot.R
\name{sample_hazard_data}
\alias{sample_hazard_data}
\title{Sample Parametric Hazard Model Predictions}
\usage{
sample_hazard_data(
  n = 500,
  time_max = 10,
  n_points = 500,
  groups = NULL,
  shape = 1.5,
  scale = 8,
  ci_level = 0.95,
  seed = 42L
)
}
\arguments{
\item{n}{Number of patients used to scale confidence-limit width.
Default \code{500}.}

\item{time_max}{Upper end of the time axis (years). Default \code{10}.}

\item{n_points}{Number of time points in the prediction grid. Default \code{500}.}

\item{groups}{\code{NULL} for a single curve, or a named numeric vector of
hazard multipliers, e.g. \code{c("Control" = 1.0, "Treatment" = 0.7)}.
A multiplier < 1 means lower hazard (better survival). Analogous to the
group indicator in \code{tp.hp.dead.tkdn.stratified.sas}.}

\item{shape}{Weibull shape parameter. \code{shape > 1} gives increasing hazard
(late mortality); \code{shape < 1} gives decreasing hazard (early operative
mortality). Default \code{1.5}.}

\item{scale}{Weibull scale parameter (characteristic time in years, i.e.
the time at which \verb{S = exp(-1) ≈ 37\%}). Default \code{8.0}.}

\item{ci_level}{Confidence level for the CI bands. Default \code{0.95}.}

\item{seed}{Random seed (unused for deterministic Weibull predictions;
kept for API consistency with other sample functions). Default \code{42}.}
}
\value{
A data frame with columns \code{time}, \code{survival}, \code{surv_lower},
\code{surv_upper}, \code{hazard}, \code{haz_lower}, \code{haz_upper}, \code{cumhaz}, and
(when \code{groups} is not \code{NULL}) \code{group}.
}
\description{
Simulates parametric survival, hazard, and cumulative-hazard predictions on
a fine time grid, matching the structure of the SAS \code{predict} dataset
produced by \code{tp.hp.dead.sas} and its variants after fitting a Weibull model
with \verb{PROC LIFEREG}.
}
\details{
\strong{SAS column mapping:}
\itemize{
\item \code{time}         ← \code{YEARS} / \code{iv_dead} (prediction grid)
\item \code{survival}     ← \code{SSURVIV} (predicted survival, 0–100 \%)
\item \code{surv_lower}   ← \code{SCLLSURV} (lower confidence limit on survival)
\item \code{surv_upper}   ← \code{SCLUSURV} (upper confidence limit on survival)
\item \code{hazard}       ← predicted hazard rate (\%/year)
\item \code{haz_lower}    ← lower confidence limit on hazard
\item \code{haz_upper}    ← upper confidence limit on hazard
\item \code{cumhaz}       ← cumulative hazard (\%; corresponds to \code{-log(S)*100})
\item \code{group}        ← group stratification variable (when \code{groups} is not \code{NULL})
}
}
\examples{
# Single-group predictions (tp.hp.dead.sas)
dat <- sample_hazard_data(n = 500, time_max = 10)
head(dat)

# Two-group predictions (tp.hp.dead.tkdn.stratified.sas)
dat2 <- sample_hazard_data(
  n = 400, time_max = 10,
  groups = c("No Takedown" = 1.0, "Takedown" = 0.65)
)
head(dat2)
}
\seealso{
\code{\link[=hazard_plot]{hazard_plot()}}, \code{\link[=sample_hazard_empirical]{sample_hazard_empirical()}}, \code{\link[=sample_life_table]{sample_life_table()}}
}
