% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda-plots.R
\name{sample_eda_data}
\alias{sample_eda_data}
\title{Sample EDA Data}
\usage{
sample_eda_data(n = 300L, year_range = c(2005L, 2020L), seed = 42L)
}
\arguments{
\item{n}{Number of patients. Default \code{300}.}

\item{year_range}{Integer vector \code{c(start, end)} for surgery years.
Default \code{c(2005L, 2020L)}.}

\item{seed}{Random seed for reproducibility. Default \code{42}.}
}
\value{
A data frame with columns:
\itemize{
\item \code{year}        — integer surgery year (discrete x for barplots)
\item \code{op_years}    — continuous years from first year in range (x for
scatterplots)
\item \code{male}        — binary 0/1 (sex)
\item \code{cabg}        — binary 0/1 (concomitant CABG)
\item \code{nyha}        — ordinal 1–4 (NYHA class)
\item \code{valve_morph} — character (valve morphology: Bicuspid / Tricuspid /
Unicuspid)
\item \code{ef}          — continuous ejection fraction (\%)
\item \code{lv_mass}     — continuous LV mass index (g/m²)
\item \code{peak_grad}   — continuous peak gradient (mmHg)
}
}
\description{
Generates a realistic mixed-type patient-level data frame for demonstrating
\code{\link[=hv_eda]{hv_eda()}} and \code{\link[=eda_select_vars]{eda_select_vars()}}. The data mimics a cardiac surgery
registry with binary, ordinal, character-categorical, and continuous
variables, plus a modest proportion of missing values.
}
\examples{
dta <- sample_eda_data()
head(dta)
sapply(dta, eda_classify_var)
}
\seealso{
\code{\link[=hv_eda]{hv_eda()}}, \code{\link[=eda_classify_var]{eda_classify_var()}}, \code{\link[=eda_select_vars]{eda_select_vars()}}
}
