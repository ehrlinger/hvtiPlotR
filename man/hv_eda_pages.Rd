% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda-pages.R
\name{hv_eda_pages}
\alias{hv_eda_pages}
\title{Prepare a paginated EDA section}
\usage{
hv_eda_pages(
  data,
  x_col = "year",
  section = c("continuous", "percent", "count"),
  vars = NULL,
  labels = NULL,
  unique_limit = 6L,
  unique_bound = 100,
  type_overrides = NULL
)
}
\arguments{
\item{data}{Data frame; one row per patient.}

\item{x_col}{Name of the reference column, usually the year of operation.
Default \code{"year"}.}

\item{section}{One of \code{"continuous"}, \code{"percent"} or \code{"count"}.}

\item{vars}{Character vector of the variables to consider, in page order.
\code{NULL} (the default) means every column except \code{x_col}. Every name is
checked at once, and the error lists all that are missing.}

\item{labels}{Optional named character vector of display labels, names
being column names. A variable without a label is shown by its name.}

\item{unique_limit, unique_bound, type_overrides}{Passed to
\code{\link[=eda_classify_var]{eda_classify_var()}}.}
}
\value{
An object of class \code{c("hv_eda_pages", "hv_data")}:
\describe{
\item{\verb{$data}}{One row per variable in the section, in page order:
\code{variable}, \code{label}, \code{var_type}.}
\item{\verb{$meta}}{Named list: \code{x_col}, \code{section}, \code{x_binned}, \code{n_vars},
\code{n_obs}, and \code{n_other}, the number of considered variables that belong
to other sections.}
\item{\verb{$tables}}{\code{panels}, a named list of \code{\link[=hv_eda]{hv_eda()}} objects, one per
variable.}
}
}
\description{
Classifies each variable with \code{\link[=eda_classify_var]{eda_classify_var()}}, keeps the ones that
belong to \code{section}, and prepares one \code{\link[=hv_eda]{hv_eda()}} panel for each. Call
\code{\link[=plot.hv_eda_pages]{plot.hv_eda_pages()}} on the result to lay the panels out as pages.
}
\details{
The three sections split one dataset by variable class:
\itemize{
\item \code{"continuous"}: variables classified \code{"Cont"}, drawn as scatter plots
against \code{x_col}.
\item \code{"percent"}: categorical variables (\code{"Cat_Num"}, \code{"Cat_Char"}), drawn as
bars filled to 100\% in each bin of \code{x_col}.
\item \code{"count"}: the same categorical variables, drawn as stacked counts.
}

\strong{The percent and count sections use the same bins.} Categorical panels
need a discrete x, so \code{x_col} is binned once, here, and every categorical
panel reads that bin. A whole-valued \code{x_col} (a calendar year) is used as
it is. A fractional one (years since an origin) is binned by \code{floor()}, so
each bar is one whole year, and \code{meta$x_binned} records that it was.
Continuous panels always use \code{x_col} unbinned.
}
\examples{
dta <- sample_eda_data(n = 300, seed = 42)

# Continuous variables, four panels to a page
cont <- hv_eda_pages(dta, x_col = "year", section = "continuous")
cont
pages <- plot(cont, ncol = 2, nrow = 2)
length(pages)
attr(pages[[1]], "variables")

# Categorical variables as percentages, decorated across the whole page
pct <- hv_eda_pages(dta, x_col = "year", section = "percent",
                    labels = c(male = "Male", nyha = "NYHA class"))
plot(pct)[[1]] &
  ggplot2::scale_fill_brewer(palette = "Set1", na.value = "grey80") &
  theme_hv_manuscript(base_size = 8)
}
\seealso{
\code{\link[=plot.hv_eda_pages]{plot.hv_eda_pages()}}, \code{\link[=hv_eda]{hv_eda()}}, \code{\link[=eda_classify_var]{eda_classify_var()}}
}
