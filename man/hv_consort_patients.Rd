% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consort-plot.R
\name{hv_consort_patients}
\alias{hv_consort_patients}
\title{Retrieve patient IDs at a CONSORT stage}
\usage{
hv_consort_patients(tracker, stage, reason = NULL)
}
\arguments{
\item{tracker}{An \code{hv_consort_tracker}.}

\item{stage}{Character — either the \code{include_col} name (e.g. \code{"eligible"})
or the stage label (case-insensitive, e.g. \code{"Eligible"}).}

\item{reason}{Optional character.  If supplied, returns patients excluded
from \emph{this} stage for the specified reason (the string must exactly match
a value in the exclusion column).  The \code{stage} arg then refers to the
stage \emph{before} the exclusion (e.g. \code{"screened"} for \code{excl_screen}).}
}
\value{
A character vector of patient IDs (from the column named in
\code{tracker$patient_id_col}).
}
\description{
Returns the IDs of patients who are active at a given stage (or who were
excluded for a specific reason at a given stage).
}
\examples{
tracker <- hv_consort_start(data.frame(id = 1:10, age = c(rep(15,3), rep(30,7))),
                             patient_id = id) |>
  hv_consort_exclude(label = "Eligible", col = "excl_screen",
                      age < 18 ~ "Age < 18")
hv_consort_patients(tracker, "eligible")
hv_consort_patients(tracker, "screened", reason = "Age < 18")

}
