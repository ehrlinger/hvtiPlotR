% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/followup-panels.R
\name{hv_followup_panels}
\alias{hv_followup_panels}
\title{Prepare a set of goodness-of-follow-up panels}
\usage{
hv_followup_panels(
  data,
  opyrs_col = "iv_opyrs",
  origin_year,
  panels,
  events = list(),
  close_date = NULL
)
}
\arguments{
\item{data}{Data frame; one row per patient.}

\item{opyrs_col}{Name of the years-since-origin interval to the operation.
Default \code{"iv_opyrs"}.}

\item{origin_year}{The calendar year \code{opyrs_col} counts from, a whole
number.}

\item{panels}{Named list of death panels. Each entry is a list with
\code{status}, the 1/0 death indicator, \code{time}, its follow-up in years, and
optionally \code{title}.}

\item{events}{Named list of non-fatal event panels, possibly empty. Each
entry is a list with \code{event} and \code{time}, the event's indicator and
interval, \code{death} and \code{death_time}, the death it competes with, and
optionally \code{label}. A flagged event counts only when it strictly precedes
death.}

\item{close_date}{\code{NULL} to estimate, or one date (a \code{Date} or a string
\code{as.Date()} reads), on or after the last operation.}
}
\value{
An object of class \code{c("hv_followup_panels", "hv_data")}:
\describe{
\item{\verb{$data}}{One row per panel, in order, death panels first:
\code{panel}, \code{type} (\code{"followup"} or \code{"event"}), \code{title}, \code{n_drawn} and
\code{n_excluded}.}
\item{\verb{$meta}}{Named list: \code{opyrs_col}, \code{origin_year}, \code{study_start},
\code{study_end}, \code{first_operation}, \code{close_date}, \code{close_source},
\code{n_obs} and \code{n_opyrs_missing}.}
\item{\verb{$tables}}{\code{panels}, a named list of \code{\link[=hv_followup]{hv_followup()}} objects.}
}
}
\description{
Checks a set of death and event panels against the data, works out the
study window they share, and prepares one \code{\link[=hv_followup]{hv_followup()}} object per panel.
Call \code{\link[=plot.hv_followup_panels]{plot.hv_followup_panels()}} on the result for the figures.
}
\details{
\strong{The study window starts on 1 January of \code{origin_year}}, because
\code{\link[=hv_followup]{hv_followup()}} draws its potential-follow-up diagonal from \code{origin_year}
whatever \code{study_start} is. It ends at the latest operation in the data.

\strong{The close date} is \code{close_date} when given. When \code{NULL} it is estimated
as the latest operation plus follow-up in the data: the date follow-up is
known to reach, which is not necessarily the date it was closed.
\code{meta$close_source} says which, so a report can too.

Every check runs before any panel is built. Every column named anywhere is
checked at once, and the error lists all that are missing. A panel name is
also its figure's name, so a name used twice, within \code{panels} or across
\code{panels} and \code{events}, is an error. The origin is checked for plausibility:
an operation before the origin, or operation years outside 1900 to next
year, is the wrong-origin mistake, which otherwise slides every point
along the x-axis without any other symptom.
}
\examples{
dta <- sample_goodness_followup_data()
fp <- hv_followup_panels(
  dta, origin_year = 1990,
  panels = list(all = list(status = "dead", time = "iv_dead", title = "All deaths")),
  events = list(ev = list(event = "ev_event", time = "iv_event", death = "deads",
                          death_time = "iv_dead", label = "Non-fatal event"))
)
fp
fp$meta$close_source
}
\seealso{
\code{\link[=plot.hv_followup_panels]{plot.hv_followup_panels()}}, \code{\link[=hv_followup]{hv_followup()}}
}
