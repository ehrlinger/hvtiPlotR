% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stacked-histogram.R
\name{sample_stacked_histogram_data}
\alias{sample_stacked_histogram_data}
\title{Generate Sample Data for Stacked Histogram}
\usage{
sample_stacked_histogram_data(
  n_years = 20,
  start_year = 2000,
  n_categories = 3,
  seed = 42
)
}
\arguments{
\item{n_years}{Integer. Number of consecutive years to simulate starting
from \code{start_year}. Defaults to `20`.}

\item{start_year}{Integer. First calendar year in the sequence. Defaults to
`2000`.}

\item{n_categories}{Integer. Number of distinct groups. Defaults to `3`.}

\item{seed}{Integer passed to \code{\link[base]{set.seed}} for
reproducibility. Defaults to `42`.}
}
\value{
A data frame with columns \code{year} (integer) and
  \code{category} (integer, 1 to \code{n_categories}).
}
\description{
Creates a minimal data frame suitable for demonstrating or testing
\code{\link{stacked_histogram}}.
}
\examples{
dta <- sample_stacked_histogram_data()
head(dta)
table(dta$year, dta$category)

}
