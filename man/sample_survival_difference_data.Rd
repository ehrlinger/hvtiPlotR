% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard-plot.R
\name{sample_survival_difference_data}
\alias{sample_survival_difference_data}
\title{Sample Survival Difference (Life-Gained) Data}
\usage{
sample_survival_difference_data(
  n = 500,
  time_max = 10,
  n_points = 500,
  groups = c(Control = 1, Treatment = 0.7),
  shape = 1.5,
  scale = 8,
  ci_level = 0.95,
  seed = 42L
)
}
\arguments{
\item{n}{Number of patients per group (used for CI width). Default \code{500}.}

\item{time_max}{Upper end of the time axis (years). Default \code{10}.}

\item{n_points}{Number of prediction grid points. Default \code{500}.}

\item{groups}{Named numeric vector of length 2; hazard multipliers for
groups 1 and 2. The group with the smaller multiplier has better survival.
Default \code{c("Control" = 1.0, "Treatment" = 0.7)}.}

\item{shape}{Weibull shape. Default \code{1.5}.}

\item{scale}{Weibull scale (years). Default \code{8.0}.}

\item{ci_level}{Confidence level. Default \code{0.95}.}

\item{seed}{Random seed. Default \code{42}.}
}
\value{
A data frame with columns \code{time}, \code{difference}, \code{diff_lower},
\code{diff_upper}, \code{group1_surv}, \code{group2_surv}.
}
\description{
Computes a group-vs-group survival difference curve and confidence interval,
matching the output of the \code{HAZDIFL} macro used in
\code{tp.hp.dead.life-gained.sas}.
}
\details{
\strong{SAS column mapping:}
\itemize{
\item \code{time}        ← prediction grid
\item \code{difference}  ← survival(group 2) - survival(group 1) (percentage points)
\item \code{diff_lower}  ← lower CI on difference
\item \code{diff_upper}  ← upper CI on difference
\item \code{group1_surv} ← survival curve for group 1
\item \code{group2_surv} ← survival curve for group 2
}
}
\examples{
diff_dat <- sample_survival_difference_data(
  groups = c("Control" = 1.0, "Treatment" = 0.7)
)
head(diff_dat)
}
\seealso{
\code{\link[=survival_difference_plot]{survival_difference_plot()}}, \code{\link[=sample_hazard_data]{sample_hazard_data()}}
}
