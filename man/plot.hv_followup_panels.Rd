% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/followup-panels.R
\name{plot.hv_followup_panels}
\alias{plot.hv_followup_panels}
\title{Plot an hv_followup_panels object}
\usage{
\method{plot}{hv_followup_panels}(x, alpha = 0.5, ...)
}
\arguments{
\item{x}{An \code{hv_followup_panels} object.}

\item{alpha}{Point transparency. Default \code{0.5}, which keeps a dense cohort
readable as a distribution.}

\item{...}{Passed to \code{\link[=plot.hv_followup]{plot.hv_followup()}}, for example \code{diagonal_color}.}
}
\value{
A named list of bare \code{ggplot} objects, one per panel, in the order
of \code{x$data}.
}
\description{
Draws one bare goodness-of-follow-up figure per panel, through
\code{\link[=plot.hv_followup]{plot.hv_followup()}}. Colours, shapes, labels and themes are left to the
caller, as everywhere in this package; the state levels are \code{"Alive"} and
\code{"Dead"} for a death panel, and \code{"No event"}, the event's label and
\code{"Death"} for an event panel.
}
\examples{
dta <- sample_goodness_followup_data()
fp <- hv_followup_panels(dta, origin_year = 1990,
                         panels = list(all = list(status = "dead", time = "iv_dead")))
plots <- plot(fp)
plots$all +
  ggplot2::scale_colour_manual(values = c(Alive = "#377EB8", Dead = "#E41A1C")) +
  ggplot2::labs(x = "Year of operation", y = "Follow-up (years)")
}
\seealso{
\code{\link[=hv_followup_panels]{hv_followup_panels()}}, \code{\link[=plot.hv_followup]{plot.hv_followup()}}
}
