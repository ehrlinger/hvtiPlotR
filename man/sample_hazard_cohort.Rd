% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard-plot.R
\name{sample_hazard_cohort}
\alias{sample_hazard_cohort}
\title{Sample Subject-Level Cohort Behind the Hazard Examples}
\usage{
sample_hazard_cohort(
  n = 500,
  time_max = 10,
  groups = NULL,
  shape = 1.5,
  scale = 8,
  seed = 42L
)
}
\arguments{
\item{n}{Number of simulated subjects \strong{per group}. Default \code{500}.}

\item{time_max}{Scale of the follow-up window (years). Default \code{10}. Note
that this is \strong{not} a hard maximum on the returned \code{time}: follow-up is
administratively truncated at \code{1.2 * time_max}, matching the cohort
\code{\link[=sample_hazard_empirical]{sample_hazard_empirical()}} fits to, so returned times run up to \code{12} at
the default. It \emph{is} the upper end of the grid in \code{\link[=sample_hazard_data]{sample_hazard_data()}}
and of the bins in \code{\link[=sample_hazard_empirical]{sample_hazard_empirical()}}.}

\item{groups}{\code{NULL} for a single group, or a named numeric vector of
hazard multipliers matching those passed to \code{\link[=sample_hazard_data]{sample_hazard_data()}}. Names
must be present, non-empty and distinct; multipliers must be finite and
greater than zero.}

\item{shape}{Weibull shape parameter. Default \code{1.5}.}

\item{scale}{Weibull scale parameter (years). Default \code{8.0}.}

\item{seed}{Random seed. Default \code{42}.}
}
\value{
A data frame with one row per subject and columns \code{time} (follow-up
time, years) and \code{status} (\code{1} event, \code{0} censored), plus a factor \code{group}
column when \code{groups} is not \code{NULL}.
}
\description{
Returns the subject-level survival data that \code{\link[=sample_hazard_empirical]{sample_hazard_empirical()}}
simulates internally and then discards. Use it when a hazard figure needs a
numbers-at-risk table: \code{\link[=hv_atrisk]{hv_atrisk()}} counts subjects still under observation
and therefore cannot work from \code{\link[=sample_hazard_data]{sample_hazard_data()}}, which is a prediction
grid with no subjects in it.
}
\details{
For a given \code{n}, \code{time_max}, \code{groups}, \code{shape}, \code{scale} and \code{seed} this draws
the \emph{same} cohort that \code{\link[=sample_hazard_empirical]{sample_hazard_empirical()}} fits its Kaplan-Meier
overlay to, so the counts and the overlay describe the same subjects. Event
times are Weibull, censoring is uniform on \verb{[0.2, 1.5] * time_max}, and
follow-up is administratively truncated at \code{1.2 * time_max}. When \code{groups} is
supplied each level is an independent draw of \code{n} subjects, matching
\code{\link[=sample_hazard_empirical]{sample_hazard_empirical()}}'s balanced-arms convention.

Note that \code{\link[=sample_hazard_data]{sample_hazard_data()}} is an \emph{analytic} Weibull curve evaluated at
the same parameters — it is not fitted to this cohort. The two share a
generative model, not an estimation step, so a figure combining them should
not be captioned as a model fit to these subjects.
}
\examples{
coh <- sample_hazard_cohort(n = 500, time_max = 10)
nrow(coh)
mean(coh$status)

# Numbers at risk under a parametric hazard figure. The cohort and the
# curve share n / shape / scale / seed, so the counts belong under it.
arms <- c("No Takedown" = 1.0, "Takedown" = 0.65)
coh2 <- sample_hazard_cohort(n = 400, time_max = 10, groups = arms)
haz  <- hv_hazard(
  sample_hazard_data(n = 400, time_max = 10, groups = arms),
  group_col = "group"
)
hv_atrisk_compose(
  plot(haz),
  hv_atrisk(coh2, time = "time", status = "status", group = "group",
            report_times = c(0, 2, 4, 6, 8, 10))
)
}
\seealso{
\code{\link[=hv_atrisk]{hv_atrisk()}}, \code{\link[=hv_atrisk_compose]{hv_atrisk_compose()}}, \code{\link[=sample_hazard_empirical]{sample_hazard_empirical()}},
\code{\link[=sample_hazard_data]{sample_hazard_data()}}
}
