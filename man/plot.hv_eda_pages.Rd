% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda-pages.R
\name{plot.hv_eda_pages}
\alias{plot.hv_eda_pages}
\title{Plot an hv_eda_pages object}
\usage{
\method{plot}{hv_eda_pages}(x, ncol = 4L, nrow = 4L, alpha = 0.5, ...)
}
\arguments{
\item{x}{An \code{hv_eda_pages} object.}

\item{ncol, nrow}{Panels across and down each page. Default 4 by 4.}

\item{alpha}{Point transparency for continuous panels. Default \code{0.5},
which keeps a dense cohort readable as a distribution.}

\item{...}{Passed to \code{\link[=plot.hv_eda]{plot.hv_eda()}}, for example \code{smooth_span}.}
}
\value{
A list of patchwork pages, possibly empty. Each page carries the
names of the variables on it as \code{attr(page, "variables")}, so a report
can caption it.
}
\description{
Lays the section's panels out as pages of \code{ncol} by \code{nrow} panels. Nothing
is printed or saved; the caller prints, saves or captions each page.
}
\details{
Colours and themes are left to the caller, as everywhere in this package.
Apply them to every panel of a page at once with patchwork's \code{&}:
\code{page & theme_hv_manuscript(base_size = 8)}.
}
\examples{
dta <- sample_eda_data(n = 300, seed = 42)
pages <- plot(hv_eda_pages(dta, section = "count"), ncol = 3, nrow = 2)
for (page in pages) message(paste(attr(page, "variables"), collapse = ", "))
}
\seealso{
\code{\link[=hv_eda_pages]{hv_eda_pages()}}, \code{\link[=plot.hv_eda]{plot.hv_eda()}}
}
