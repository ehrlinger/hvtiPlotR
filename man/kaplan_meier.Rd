% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kaplan-meier.R
\name{kaplan_meier}
\alias{kaplan_meier}
\alias{km}
\alias{kaplan}
\title{Kaplan-Meier Survival Curve (alias)}
\usage{
kaplan_meier(
  data,
  time_col = "iv_dead",
  event_col = "dead",
  strata_col = NULL,
  conf_int = TRUE,
  conf_level = 0.6827,
  report_times = c(1, 5, 10, 15, 20, 25),
  alpha = 0.8
)

km(
  data,
  time_col = "iv_dead",
  event_col = "dead",
  strata_col = NULL,
  conf_int = TRUE,
  conf_level = 0.6827,
  report_times = c(1, 5, 10, 15, 20, 25),
  alpha = 0.8
)

kaplan(
  data,
  time_col = "iv_dead",
  event_col = "dead",
  strata_col = NULL,
  conf_int = TRUE,
  conf_level = 0.6827,
  report_times = c(1, 5, 10, 15, 20, 25),
  alpha = 0.8
)
}
\arguments{
\item{data}{A data frame.}

\item{time_col}{Name of the numeric column holding follow-up time in
years. Defaults to \code{"iv_dead"}.}

\item{event_col}{Name of the logical or 0/1 column indicating whether
the event occurred. Defaults to \code{"dead"}.}

\item{strata_col}{Optional name of a character or factor column used to
stratify the analysis. Pass \code{NULL} (the default) for an unstratified
estimate.}

\item{conf_int}{Logical; draw a confidence-interval ribbon on the
survival plot. Defaults to \code{TRUE}.}

\item{conf_level}{Confidence level for the CI band. Defaults to
\code{0.6827} (one standard deviation), matching the SAS \code{\%kaplan}
macro default.}

\item{report_times}{Numeric vector of time points at which survival
estimates and numbers at risk are reported. Defaults to
\code{c(1, 5, 10, 15, 20, 25)}.}

\item{alpha}{Transparency of plot lines and points, in [0, 1].
Default \code{0.8}. The CI ribbon uses a fixed transparency of \code{0.2}.}
}
\value{
See \code{\link[=survival_curve]{survival_curve()}}.
}
\description{
Calls \code{\link[=survival_curve]{survival_curve()}} with \code{method = "kaplan-meier"}. See that function
for the full parameter list and return value.
}
\seealso{
\code{\link[=survival_curve]{survival_curve()}}, \code{\link[=nelsont]{nelsont()}}
}
