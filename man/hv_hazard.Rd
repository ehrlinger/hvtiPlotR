% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard-plot.R
\name{hv_hazard}
\alias{hv_hazard}
\title{Prepare parametric hazard / survival data for plotting}
\usage{
hv_hazard(
  curve_data,
  x_col = "time",
  estimate_col = "survival",
  lower_col = NULL,
  upper_col = NULL,
  group_col = NULL,
  empirical = NULL,
  emp_x_col = x_col,
  emp_estimate_col = "estimate",
  emp_lower_col = NULL,
  emp_upper_col = NULL,
  emp_group_col = group_col,
  reference = NULL,
  ref_x_col = x_col,
  ref_estimate_col = estimate_col,
  ref_group_col = NULL
)
}
\arguments{
\item{curve_data}{Data frame of parametric predictions (fine grid).
Typical source: SAS \code{predict} dataset exported to CSV, or
\code{\link[=sample_hazard_data]{sample_hazard_data()}}.}

\item{x_col}{Name of the time / age column. Default \code{"time"}.}

\item{estimate_col}{Name of the predicted-value column: \code{"survival"},
\code{"hazard"}, or \code{"cumhaz"}. Default \code{"survival"}.}

\item{lower_col}{Lower CI column in \code{curve_data}, or \code{NULL} for no
ribbon. Default \code{NULL}.}

\item{upper_col}{Upper CI column in \code{curve_data}, or \code{NULL}. Default
\code{NULL}.}

\item{group_col}{Stratification column in \code{curve_data}, or \code{NULL} for
a single curve. Default \code{NULL}.}

\item{empirical}{Optional data frame of KM empirical points (SAS
\code{plout} dataset). Stored in \verb{$tables$empirical}. Default \code{NULL}.}

\item{emp_x_col}{x column in \code{empirical}. Defaults to \code{x_col}.}

\item{emp_estimate_col}{y column in \code{empirical}. Default \code{"estimate"}.}

\item{emp_lower_col}{Lower error-bar column in \code{empirical}, or \code{NULL}.
Default \code{NULL}.}

\item{emp_upper_col}{Upper error-bar column in \code{empirical}, or \code{NULL}.
Default \code{NULL}.}

\item{emp_group_col}{Group column in \code{empirical}. Defaults to \code{group_col}.}

\item{reference}{Optional data frame of population life-table curves
(SAS \code{smatched} dataset). Stored in \verb{$tables$reference}. Default \code{NULL}.}

\item{ref_x_col}{x column in \code{reference}. Defaults to \code{x_col}.}

\item{ref_estimate_col}{y column in \code{reference}. Defaults to \code{estimate_col}.}

\item{ref_group_col}{Linetype-grouping column in \code{reference}, or \code{NULL}.
Default \code{NULL}.}
}
\value{
An S3 object of class \code{c("hv_hazard", "hv_data")}; call
\code{plot()} on the result to render — see \code{\link[=plot.hv_hazard]{plot.hv_hazard()}}. The object
contains: \verb{$data} (curve data frame), \verb{$meta} (all column-name mappings),
\verb{$tables$empirical}, \verb{$tables$reference}.
}
\description{
Validates and stores pre-computed parametric curve data — and optional
Kaplan-Meier empirical overlay and population life-table reference — as an
\code{hv_hazard} object.  Pass the result to \code{\link[=plot.hv_hazard]{plot.hv_hazard()}} to render
the figure.
}
\details{
This constructor covers the complete \verb{tp.hp.dead.*} SAS template family.
All column-name mappings and all three data frames are fixed at construction
time; aesthetic parameters (\code{ci_alpha}, \code{line_width}, etc.) are passed to
\code{plot()}.
}
\examples{
library(ggplot2)

dat <- sample_hazard_data(n = 500, time_max = 10)
emp <- sample_hazard_empirical(n = 500, time_max = 10, n_bins = 6)

# Basic survival curve with KM overlay
hp <- hv_hazard(dat,
  lower_col     = "surv_lower", upper_col = "surv_upper",
  empirical     = emp,
  emp_lower_col = "lower",     emp_upper_col = "upper"
)
plot(hp) +
  scale_colour_manual(values = c("steelblue"), guide = "none") +
  scale_fill_manual(values   = c("steelblue"), guide = "none") +
  scale_x_continuous(limits = c(0, 10), breaks = 0:10) +
  scale_y_continuous(limits = c(0, 100), breaks = seq(0, 100, 20),
                     labels = function(x) paste0(x, "\%")) +
  labs(x = "Years", y = "Survival (\%)") +
  hv_theme("manuscript")

# Stratified groups
dat2 <- sample_hazard_data(
  n = 400, groups = c("No Takedown" = 1.0, "Takedown" = 0.65)
)
hp2 <- hv_hazard(dat2,
  lower_col = "surv_lower", upper_col = "surv_upper",
  group_col = "group"
)
plot(hp2) +
  scale_colour_manual(
    values = c("No Takedown" = "steelblue", "Takedown" = "firebrick"),
    name   = NULL
  ) +
  labs(x = "Years", y = "Survival (\%)") +
  hv_theme("manuscript")

# --- Global theme + RColorBrewer (set once per session) ------------------
\dontrun{
# Apply manuscript theme globally; use scale_colour_brewer for groups.
old <- ggplot2::theme_set(hv_theme_manuscript())
plot(hp2) +
  scale_colour_brewer(palette = "Set1", name = NULL) +
  scale_fill_brewer(palette   = "Set1", guide = "none") +
  scale_x_continuous(limits = c(0, 10), breaks = 0:10) +
  scale_y_continuous(limits = c(0, 100), breaks = seq(0, 100, 20),
                     labels = function(x) paste0(x, "\%")) +
  labs(x = "Years", y = "Survival (\%)")
ggplot2::theme_set(old)
}

# See vignette("plot-decorators", package = "hvtiPlotR") for theming,
# colour scales, annotation labels, and saving plots.

}
\seealso{
\code{\link[=plot.hv_hazard]{plot.hv_hazard()}} to render as a ggplot2 figure,
\code{\link[=hv_theme]{hv_theme()}} for the publication theme,
\code{\link[=sample_hazard_data]{sample_hazard_data()}}, \code{\link[=sample_hazard_empirical]{sample_hazard_empirical()}},
\code{\link[=sample_life_table]{sample_life_table()}} for example data.

Other Hazard plot: 
\code{\link{plot.hv_hazard}()}
}
\concept{Hazard plot}
