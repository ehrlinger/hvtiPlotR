% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spaghetti-plot.R
\name{plot.hvti_spaghetti}
\alias{plot.hvti_spaghetti}
\title{Plot an hvti_spaghetti object}
\usage{
\method{plot}{hvti_spaghetti}(
  x,
  line_colour = "grey50",
  line_width = 0.2,
  alpha = 0.8,
  add_smooth = FALSE,
  smooth_method = "loess",
  smooth_se = FALSE,
  smooth_width = 1.2,
  y_labels = NULL,
  ...
)
}
\arguments{
\item{x}{An \code{hvti_spaghetti} object.}

\item{line_colour}{Fixed line colour used when \code{colour_col = NULL}.
Default \code{"grey50"}.}

\item{line_width}{Line width for individual trajectories. Default \code{0.2}.}

\item{alpha}{Transparency of plot elements in \eqn{[0,1]}.
Default \code{0.8}.}

\item{add_smooth}{Logical; overlay a smoother? Default \code{FALSE}.}

\item{smooth_method}{Smoothing method passed to
\code{geom_smooth()}.  Default \code{"loess"}.}

\item{smooth_se}{Logical; show CI ribbon around smooth?
Default \code{FALSE}.}

\item{smooth_width}{Line width for the smooth overlay. Default \code{1.2}.}

\item{y_labels}{Named numeric vector for an ordinal y-axis, e.g.
\code{c(None = 0, Mild = 1, Moderate = 2, Severe = 3)}.
Default \code{NULL} (standard numeric axis).}

\item{...}{Ignored; present for S3 consistency.}
}
\value{
A bare \code{\link[ggplot2]{ggplot}} object; compose with \code{+}
to add scales, axis limits, labels, and \code{\link{hvti_theme}}.
}
\description{
Draws one trajectory line per subject over time, optionally stratified by
colour and with a LOESS (or other) smooth overlay.
}
\examples{
dta <- sample_spaghetti_data(n_patients = 150, seed = 42)
sp  <- hvti_spaghetti(dta, colour_col = "group")

# With LOESS smooth overlay
plot(sp, add_smooth = TRUE) +
  ggplot2::scale_colour_brewer(palette = "Set1", name = NULL) +
  ggplot2::labs(x = "Years", y = "AV Mean Gradient (mmHg)") +
  hvti_theme("manuscript")

# Ordinal y-axis
dta_ord <- dta
dta_ord$value <- round(pmin(3, pmax(0, dta$value / 12)))
plot(hvti_spaghetti(dta_ord, colour_col = "group"),
     y_labels = c(None = 0, Mild = 1, Moderate = 2, Severe = 3)) +
  ggplot2::labs(x = "Years", y = "MR Grade") +
  hvti_theme("manuscript")

# --- Global theme (set once per session) ----------------------------------
\dontrun{
old <- ggplot2::theme_set(hvti_theme_manuscript())
plot(sp, add_smooth = TRUE) +
  ggplot2::scale_colour_brewer(palette = "Set1", name = NULL) +
  ggplot2::labs(x = "Years", y = "AV Mean Gradient (mmHg)")
ggplot2::theme_set(old)
}

# See vignette("plot-decorators", package = "hvtiPlotR") for theming,
# colour scales, annotation labels, and saving plots.

}
\seealso{
\code{\link{hvti_spaghetti}} to build the data object,
\code{\link{hvti_theme}} for the publication theme.

Other Spaghetti plot: 
\code{\link{hvti_spaghetti}()}
}
\concept{Spaghetti plot}
