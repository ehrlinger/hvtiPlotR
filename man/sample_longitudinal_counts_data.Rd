% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/longitudinal-counts-plot.R
\name{sample_longitudinal_counts_data}
\alias{sample_longitudinal_counts_data}
\title{Sample Longitudinal Counts Data}
\usage{
sample_longitudinal_counts_data(n_patients = 300, max_obs = 6, seed = 42L)
}
\arguments{
\item{n_patients}{Number of unique patients passed to
\code{\link[=sample_spaghetti_data]{sample_spaghetti_data()}}. Default \code{300}.}

\item{max_obs}{Maximum observations per patient passed to
\code{\link[=sample_spaghetti_data]{sample_spaghetti_data()}}. Default \code{6}.}

\item{seed}{Random seed. Default \code{42}.}
}
\value{
A data frame in long format with columns:
\itemize{
\item \code{time_label} — ordered factor of follow-up windows
\item \code{series}     — \code{"Patients"} or \code{"Measurements"}
\item \code{count}      — integer count
}
}
\description{
Builds a pre-aggregated summary data frame of patient and measurement counts
at discrete follow-up time windows. The counts are derived by binning the
continuous \code{time} column from \code{\link[=sample_spaghetti_data]{sample_spaghetti_data()}}, so the two
functions share the same underlying simulation.
}
\examples{
dta <- sample_longitudinal_counts_data(n_patients = 300, seed = 42)
dta
}
\seealso{
\code{\link[=longitudinal_counts_plot]{longitudinal_counts_plot()}}, \code{\link[=sample_spaghetti_data]{sample_spaghetti_data()}}
}
