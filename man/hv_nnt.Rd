% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazard-plot.R
\name{hv_nnt}
\alias{hv_nnt}
\title{Prepare number-needed-to-treat data for plotting}
\usage{
hv_nnt(
  nnt_data,
  x_col = "time",
  estimate_col = "nnt",
  lower_col = NULL,
  upper_col = NULL,
  group_col = NULL,
  na_rm = TRUE
)
}
\arguments{
\item{nnt_data}{Data frame of pre-computed NNT estimates.
See \code{\link[=sample_nnt_data]{sample_nnt_data()}}.}

\item{x_col}{Name of the time column. Default \code{"time"}.}

\item{estimate_col}{Name of the NNT (or ARR) column. Default \code{"nnt"}.}

\item{lower_col}{Lower CI column, or \code{NULL}. Default \code{NULL}.}

\item{upper_col}{Upper CI column, or \code{NULL}. Default \code{NULL}.}

\item{group_col}{Grouping column for multiple comparisons, or \code{NULL}.
Default \code{NULL}.}

\item{na_rm}{Remove rows where \code{estimate_col} is \code{NA} before storing
in \verb{$data}. Applied at construction time. Default \code{TRUE}.}
}
\value{
An S3 object of class \code{c("hv_nnt", "hv_data")}.
}
\description{
Stores pre-computed NNT (or ARR) data as an \code{hv_nnt} object.  Rows where
\code{estimate_col} is \code{NA} (undefined when ARR is near zero) are optionally
removed during construction via \code{na_rm}.  Pass the result to
\code{\link[=plot.hv_nnt]{plot.hv_nnt()}} to render the plot.  Covers the NNT component of
\code{tp.hp.numtreat.survdiff.matched.sas}.
}
\examples{
library(ggplot2)

nnt_dat <- sample_nnt_data(
  n = 500, time_max = 20,
  groups = c("SVG" = 1.0, "ITA" = 0.75)
)

# 1. Build data object
nn <- hv_nnt(nnt_dat, lower_col = "nnt_lower", upper_col = "nnt_upper")
nn  # prints estimate column and CI flags

# 2. Bare plot -- undecorated ggplot returned by plot.hv_nnt
p <- plot(nn)

# 3. Decorate: axis scales, labels, theme
p +
  scale_x_continuous(limits = c(0, 20), breaks = seq(0, 20, 5)) +
  scale_y_continuous(limits = c(0, 50), breaks = seq(0, 50, 10)) +
  labs(x = "Years", y = "Number Needed to Treat (NNT)") +
  theme_hv_poster()

# ARR curve -- same 3-step pattern with a different estimate column
ar <- hv_nnt(nnt_dat, estimate_col = "arr",
               lower_col = "arr_lower", upper_col = "arr_upper",
               na_rm = FALSE)
plot(ar) +
  scale_y_continuous(limits = c(0, 50),
                     labels = function(x) paste0(x, "\%")) +
  labs(x = "Years", y = "Absolute Risk Reduction (\%)") +
  theme_hv_poster()

}
\seealso{
\code{\link[=plot.hv_nnt]{plot.hv_nnt()}}, \code{\link[=sample_nnt_data]{sample_nnt_data()}},
\code{\link[=hv_survival_difference]{hv_survival_difference()}}
}
