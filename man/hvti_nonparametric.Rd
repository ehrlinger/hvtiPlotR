% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonparametric-curve-plot.R
\name{hvti_nonparametric}
\alias{hvti_nonparametric}
\title{Prepare nonparametric temporal trend curve data for plotting}
\usage{
hvti_nonparametric(
  curve_data,
  x_col = "time",
  estimate_col = "estimate",
  lower_col = NULL,
  upper_col = NULL,
  group_col = NULL,
  data_points = NULL
)
}
\arguments{
\item{curve_data}{Data frame; one row per (time, group) combination.}

\item{x_col}{Name of the x-axis column. Default \code{"time"}.}

\item{estimate_col}{Name of the predicted value column. Default
\code{"estimate"}.}

\item{lower_col}{Name of the lower CI bound column, or \code{NULL}.
Default \code{NULL}.}

\item{upper_col}{Name of the upper CI bound column, or \code{NULL}.
Default \code{NULL}.}

\item{group_col}{Name of the stratification column, or \code{NULL}.
Default \code{NULL}.}

\item{data_points}{Optional data frame of binned data summary points.
Must have columns matching \code{x_col} and \code{"value"}, plus
\code{group_col} when stratified. Default \code{NULL}.}
}
\value{
An object of class \code{c("hvti_nonparametric", "hvti_data")}:
\describe{
\item{\code{$data}}{The \code{curve_data} data frame.}
\item{\code{$meta}}{Named list: \code{x_col}, \code{estimate_col},
\code{lower_col}, \code{upper_col}, \code{group_col},
\code{has_ci}, \code{has_data_points}, \code{n_obs}.}
\item{\code{$tables}}{List; contains \code{data_points} when supplied.}
}
}
\description{
Validates pre-computed curve data (and optional CI bounds and binned data
summary points) and returns an \code{hvti_nonparametric} object.  Call
\code{\link{plot.hvti_nonparametric}} to obtain a bare \code{ggplot2}
curve plot that you can decorate with colour/fill scales, axis limits, and
\code{\link{hvti_theme}}.
}
\details{
Covers the full range of \code{tp.np.*} SAS templates:\tabular{ll}{
   SAS template pattern \tab R usage \cr
   Single average curve (\code{avrg_curv}, \code{u.trend}) \tab \code{hvti_nonparametric(dat)} \cr
   Curve + 68\\% CI (\code{avrg_curv.ci}) \tab \code{+ lower_col + upper_col} \cr
   Curve + CI + data points \tab \code{+ data_points = ...} \cr
   Two-group comparison (\code{double}, \code{ozak}) \tab \code{+ group_col = "group"} \cr
   Multi-scenario / covariate-adjusted (\code{mult}) \tab \code{+ group_col = "group"} \cr
   Phase decomposition (\code{phases}, \code{independence}) \tab \code{+ group_col = "phase"} \cr
}


\strong{SAS column mapping:}
\itemize{
\item \code{estimate_col} ← \code{prev}, \code{mnprev}, \code{_p_},
\code{est_fev}, \code{est_z0d}
\item \code{lower_col} ← \code{cll_p68} or \code{cll_p95}
\item \code{upper_col} ← \code{clu_p68} or \code{clu_p95}
\item \code{group_col} ← indicator added after wide-to-long reshape
}
}
\examples{
dat     <- sample_nonparametric_curve_data(n = 500, time_max = 12)
dat_pts <- sample_nonparametric_curve_points(n = 500, time_max = 12)
np <- hvti_nonparametric(dat, lower_col = "lower", upper_col = "upper",
                          data_points = dat_pts)
np   # prints CI / data-point flags

plot(np) +
  ggplot2::scale_colour_manual(values = c("steelblue"), guide = "none") +
  ggplot2::scale_fill_manual(values = c("steelblue"), guide = "none") +
  ggplot2::scale_x_continuous(limits = c(0, 12), breaks = 0:12) +
  ggplot2::scale_y_continuous(limits = c(0, 0.40),
                              breaks = seq(0, 0.40, 0.10),
                              labels = scales::percent) +
  ggplot2::labs(x = "Months", y = "Prevalence of AF") +
  hvti_theme("manuscript")

}
\seealso{
\code{\link{plot.hvti_nonparametric}},
\code{\link{sample_nonparametric_curve_data}}
}
