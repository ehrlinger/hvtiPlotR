% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nonparametric-curve-plot.R
\name{sample_nonparametric_curve_data}
\alias{sample_nonparametric_curve_data}
\title{Sample Nonparametric Curve Data}
\usage{
sample_nonparametric_curve_data(
  n = 500,
  time_max = 12,
  n_points = 500,
  groups = NULL,
  outcome_type = c("probability", "continuous"),
  ci_level = 0.68,
  n_bins = 10,
  seed = 42L
)
}
\arguments{
\item{n}{Number of simulated patients (used for binned data
points and CI width). Default \code{500}.}

\item{time_max}{Upper end of the time axis (same units as the SAS
\code{iv_echo} / \code{iv_wristm} variable). Default \code{12}.}

\item{n_points}{Number of time points on the fine prediction grid.
Default \code{500} (matches the SAS \code{inc=(max-min)/499.9} loop).}

\item{groups}{\code{NULL} for a single average curve, or a named numeric
vector of group-specific hazard multipliers,
e.g. \code{c("Ozaki" = 0.8, "CE-Pericardial" = 1.2)}.}

\item{outcome_type}{\code{"probability"} (binary outcome, 0-1 scale) or
\code{"continuous"}. Default \code{"probability"}.}

\item{ci_level}{Confidence level for bootstrap-style CI bands.
Default \code{0.68}.}

\item{n_bins}{Number of equal-sized data-summary bins. Default \code{10}.}

\item{seed}{Random seed. Default \code{42}.}
}
\value{
A data frame with columns \code{time}, \code{estimate}, \code{lower}, \code{upper},
and (if \code{groups} is not \code{NULL}) \code{group}.
}
\description{
Simulates pre-computed curve output matching what SAS produces after fitting
a two-phase nonparametric temporal trend model and averaging patient-specific
profiles with \verb{PROC SUMMARY}. The output is suitable for direct use with
\code{\link[=hv_nonparametric]{hv_nonparametric()}}.
}
\details{
\strong{SAS context:} In the SAS templates this dataset corresponds to
\code{mean_curv} (estimate column) plus \code{boots_ci} (lower/upper columns).
Export those datasets to CSV and read them with \code{\link[=read.csv]{read.csv()}} to use your
own model output instead of this sample function.
}
\examples{
# Single average curve
dat <- sample_nonparametric_curve_data(n = 500, time_max = 12)
head(dat)

# Two-group comparison
dat2 <- sample_nonparametric_curve_data(
  n = 400, time_max = 7,
  groups = c("Ozaki" = 0.7, "CE-Pericardial" = 1.3),
  outcome_type = "continuous"
)
head(dat2)
}
\seealso{
\code{\link[=hv_nonparametric]{hv_nonparametric()}}, \code{\link[=sample_nonparametric_curve_points]{sample_nonparametric_curve_points()}}
}
