% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trends-plot.R
\name{sample_trends_data}
\alias{sample_trends_data}
\title{Sample Temporal Trend Data}
\usage{
sample_trends_data(
  n = 600,
  year_range = c(1990L, 2020L),
  groups = c("Group I", "Group II", "Group III", "Group IV"),
  seed = 42L
)
}
\arguments{
\item{n}{Total number of patients. Default \code{600}.}

\item{year_range}{Integer vector \code{c(start, end)} for the x-axis range
(surgery year, or patient age when used with \code{tp.lp.trends.age.sas}
patterns). Default \code{c(1990, 2020)}.}

\item{groups}{Character vector of group labels, or \code{NULL} for a
single-group figure (no \code{group} column returned; use with
\code{trends_plot(..., group_col = NULL)}). Default
\code{c("Group I", "Group II", "Group III", "Group IV")}.}

\item{seed}{Random seed for reproducibility. Default \code{42}.}
}
\value{
A data frame with columns:
\itemize{
\item \code{year}  — x-axis value (integer; surgery year or patient age)
\item \code{value} — continuous outcome (numeric)
\item \code{group} — group label (factor, ordered by \code{groups}); absent when
\code{groups = NULL}
}
}
\description{
Generates a realistic patient-level longitudinal data set for demonstrating
\code{\link[=trends_plot]{trends_plot()}}. Each row is one patient with a surgery year, continuous
outcome (\code{value}), and a grouping variable (\code{group}). Trend patterns are
modelled so that group means diverge over time — matching the multi-group
NYHA / LV-mass / LOS pattern in the SAS template.
}
\examples{
dta <- sample_trends_data(n = 400, seed = 42)
head(dta)
table(dta$year, dta$group)
}
\seealso{
\code{\link[=trends_plot]{trends_plot()}}
}
