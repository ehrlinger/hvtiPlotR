% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kaplan-meier.R
\name{km_build_hazard_plot}
\alias{km_build_hazard_plot}
\title{Build bare hazard rate ggplot}
\usage{
km_build_hazard_plot(km_df, alpha)
}
\arguments{
\item{km_df}{Tidy KM data frame from \code{km_extract_tidy}.}
}
\value{
A bare \code{ggplot} object.
}
\description{
Plots the instantaneous hazard estimates computed using the SAS
\code{\%kaplan} formula: \eqn{h(t) = \log(S(t_{prev}) / S(t)) / \Delta t},
plotted at the interval midpoint.  Only event times with
\eqn{\Delta t > 0} and \eqn{S(t) > 0} are shown; censoring rows are
excluded.  A smoother (e.g., \code{geom_smooth}) is typically added by
the caller.
}
\keyword{internal}
