% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_light_ppt.R
\name{hv_theme_light_ppt}
\alias{hv_theme_light_ppt}
\alias{theme_light_ppt}
\title{Light PowerPoint Theme}
\usage{
hv_theme_light_ppt(
  base_size = 32,
  base_family = "",
  header_family = NULL,
  base_line_size = base_size/22,
  base_rect_size = base_size/22,
  ink = "black",
  paper = "transparent",
  accent = "#3366FF",
  bold = FALSE
)

theme_light_ppt(
  base_size = 32,
  base_family = "",
  header_family = NULL,
  base_line_size = base_size/22,
  base_rect_size = base_size/22,
  ink = "black",
  paper = "transparent",
  accent = "#3366FF",
  bold = FALSE
)
}
\arguments{
\item{base_size}{Base font size in points.
Default \code{32}.}

\item{base_family}{Base font family. Default \code{""} (device default).}

\item{header_family}{Font family for headers, or \code{NULL} to inherit
\code{base_family}. Default \code{NULL}.}

\item{base_line_size}{Line size used for axis lines and borders.
Default \code{base_size / 22}.}

\item{base_rect_size}{Rectangle border size. Default \code{base_size / 22}.}

\item{ink}{Foreground (text and line) colour. Default \code{"black"}.}

\item{paper}{Background colour. Default \code{"transparent"}.}

\item{accent}{Accent colour used by some \code{theme_grey()} elements.
Default \code{"#3366FF"}.}

\item{bold}{If \code{TRUE}, axis text and axis titles are rendered
with \code{face = "bold"}. Default \code{FALSE}.}
}
\value{
A \code{\link[ggplot2:theme]{ggplot2::theme()}} object.
}
\description{
A large-font theme with a \strong{white/transparent background} suited to
light-background PowerPoint slides (e.g. AATS-style). Removes grid lines
and panel borders.
}
\details{
For the default dark-background PowerPoint theme use
\code{\link[=hv_theme_dark_ppt]{hv_theme_dark_ppt()}} or its alias \code{hv_theme_ppt()}.

Legend is hidden by default since PowerPoint figures are typically
annotated directly on the panel; add \code{+ theme(legend.position = "right")}
(or similar) to override. Axis-text and axis-title margins are scaled from
\code{base_size} via ggplot2's \code{half_line = base_size / 2} convention, so the
spacing stays proportional when \code{base_size} changes.
}
\examples{
library(ggplot2)
p <- ggplot(mtcars, aes(wt, mpg, colour = factor(cyl))) + geom_point()

# Light PPT theme — large font, black text, transparent background
p + hv_theme_light_ppt()

# Via alias
p + theme_light_ppt()

# Bold axis text/titles
p + hv_theme_light_ppt(bold = TRUE)

# Via the generic dispatcher
p + hv_theme("light_ppt")

}
\seealso{
\code{\link[=hv_theme]{hv_theme()}}, \code{\link[=hv_theme_dark_ppt]{hv_theme_dark_ppt()}}, \code{\link[=theme_man]{theme_man()}},
\code{\link[=theme_poster]{theme_poster()}}
}
