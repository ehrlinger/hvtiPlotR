% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consort-plot.R
\name{hv_consort_exclude}
\alias{hv_consort_exclude}
\title{Add an exclusion stage to a CONSORT tracker}
\usage{
hv_consort_exclude(
  tracker,
  label,
  col,
  ...,
  excl_label = "Excluded",
  pass_col = NULL
)
}
\arguments{
\item{tracker}{An \code{hv_consort_tracker} from \code{\link[=hv_consort_start]{hv_consort_start()}}.}

\item{label}{Character label for the survivor box after this exclusion
(e.g. \code{"Eligible"}, \code{"Analyzed"}).}

\item{col}{Column name to store exclusion reasons (character).
This column will contain a reason string for excluded patients and \code{NA}
for survivors.  Required -- no default.}

\item{...}{Two-sided formulas of the form
\verb{<condition> ~ "<reason string>"}.  Conditions are evaluated with data
masking against the tracker's data frame.  The \strong{first} matching formula
assigns the reason; subsequent formulas are not evaluated for already-
excluded patients.}

\item{excl_label}{Character label for the side-box showing exclusion
breakdown.  Default \code{"Excluded"}.}

\item{pass_col}{Column name for the survivor boolean column.  Defaults to
\code{ct_snakify(label)} (e.g. \code{"eligible"} when \code{label = "Eligible"}).}
}
\value{
The updated \code{hv_consort_tracker} (invisibly -- pipe-safe).
}
\description{
Evaluates formula-based exclusion rules against the currently-active patient
population and appends two new columns to the tracker's data frame:
a character column (\code{col}) recording the first-matching exclusion reason for
each patient, and a boolean column (\code{pass_col}) marking the survivors.
Patients already excluded by a prior stage are automatically gated out.
}
\examples{
cohort <- data.frame(
  mrn  = paste0("P", 1:100),
  age  = sample(15:80, 100, TRUE),
  echo = sample(c(TRUE, FALSE), 100, TRUE, prob = c(0.9, 0.1))
)

tracker <- hv_consort_start(cohort, patient_id = mrn) |>
  hv_consort_exclude(
    label    = "Eligible",
    col      = "excl_screen",
    age < 18 ~ "Age < 18"
  ) |>
  hv_consort_exclude(
    label  = "Analyzed",
    col    = "excl_eligible",
    !echo  ~ "Missing echocardiogram"
  )

}
\seealso{
\code{\link[=hv_consort_start]{hv_consort_start()}}, \code{\link[=hv_consort]{hv_consort()}}
}
